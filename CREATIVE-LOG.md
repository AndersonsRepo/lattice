# Lattice Creative Log

Tracks what's been built in each creative cycle so work doesn't repeat.

## Completed

### 2026-03-21 — Petri / Reaction-Diffusion Laboratory
- **What**: Full-screen interactive Gray-Scott reaction-diffusion playground (`docs/petri.html`) — paint living chemicals and watch patterns self-organize
- **Details**: WebGL ping-pong framebuffer simulation at 512x512. Two chemicals (U activator, V inhibitor) diffuse and react according to the Gray-Scott model. 9-point weighted Laplacian for smooth diffusion. **Brushes**: Seed (inject chemical V) and Erase (restore to U=1, V=0) with adjustable radius and smooth falloff. **8 curated presets**: Mitosis, Coral, Spirals, Spots, Stripes, Worms, Maze, Bubbles — each a known-beautiful parameter region. **4 tunable parameters**: feed rate, kill rate, Du/Dv diffusion coefficients. **Speed control**: 1-40 simulation steps per frame. **8 color palettes**: Void (purple), Ember, Ocean, Neon, Mono, Blood, Frost, Lattice (project colors). 5-stop gradient color mapping with vignette. **Audio reactivity**: microphone input modulates feed/kill rates — bass shifts feed, treble shifts kill, with adjustable mix. Frequency spectrum bar. **Interaction**: click+drag to paint, scroll wheel for brush size, touch support. **Export**: PNG download. Keyboard shortcuts: Space (pause), H (panel), C (clear), R (scatter seeds), S (export), M (mic), B (toggle brush), 1-8 (presets). Frosted-glass collapsible panel. ~32KB total, no dependencies beyond WebGL.
- **Pages**: `docs/petri.html` (new), `docs/index.html` (added Petri button)
- **What makes it unique**: Reaction-diffusion is one of Lattice's core evolutionary engines, but no existing page lets you directly interact with the simulation. Create treats it as one algorithm among many with preset parameters. Petri makes the chemistry itself the art — you paint chemicals like an oil painter, and the mathematics decides what grows. The parameter space is vast: tiny changes in feed/kill produce entirely different morphologies (spots vs stripes vs coral vs worms). Audio reactivity means your voice or music literally shifts the chemistry.

### 2026-03-21 — Performance Pipeline v4 / Full Worker Offloading + Double-Buffering
- **What**: Profiled and optimized the entire evolution rendering pipeline. All 11 engines now run in WebWorkers with double-buffered Transferable ArrayBuffers — main thread does zero simulation work.
- **Bugs fixed**: `live.html` called undefined `createEngine()` crashing non-heavy engines. `perf.html` referenced non-existent `WorkerSim` breaking benchmark.
- **Architecture**: WorkerBridge `autoStep` double-buffering, BufferPool, 4x unrolled fill. ALL engines off main thread. Adaptive FrameBudget. R-D modulo elimination, FlowField incremental max tracking, Attractor log tone mapping.
- **Before/After**: 7/11 engines blocked main thread → all 11 off-thread. Main thread sim cost: 0ms.
- **Pages**: `docs/lattice-perf.js`, `docs/lattice-worker.js`, `docs/live.html`, `docs/perf.html`

### 2026-03-21 — Choir / Evolutionary Ensemble
- **What**: The entire population sings as one — every Hall of Fame and current piece plays simultaneously as a voice in a living choir (`docs/choir.html`)
- **Details**: Loads all HoF pieces + current population + recent archive from gallery.json. Each piece becomes a Voice with genome-derived audio: type determines waveform/filter (square for 1D/WFC, triangle for 2D/voronoi, sawtooth for lsystem/attractor, sine for RD/julia), fitness determines volume, metrics shape rhythm patterns (edge activity → note density, symmetry → pattern mirroring), complexity+density → pitch, novelty → melodic range, fractal dimension → filter sweep. Web Audio API with per-voice oscillator+filter chains, dynamics compressor, algorithmic convolution reverb, sub-bass C2 drone. Four constellation formations: **Constellation** (X=complexity, Y=fitness), **Circle**, **Grid**, **Spiral** — positions animate with spring physics. Lineage lines connect parent-child pieces. Hover any node to see piece ID/score/type/generation; press S to solo that voice. Real-time frequency spectrum. Controls: volume, tempo (20-200 BPM), spread, reverb wet/dry.
- **Pages**: `docs/choir.html` (new), `docs/index.html` (added Choir button + footer link)
- **How it differs**: Every other sound page sonifies pieces individually or sequentially. Choir is the only page that plays the entire population simultaneously as a polyphonic ensemble. The composition IS the evolutionary state — different populations produce fundamentally different music. Lineage becomes audible: related pieces share pitch ranges while divergent species create harmonic tension.

### 2026-03-21 — Aether / Interactive Flow Field Explorer
- **What**: Full-screen particle flow field explorer (`docs/aether.html`) — thousands of particles carried by invisible mathematical fields, shaped by mouse interaction
- **Details**: 8000 particles (1K-25K) in 4 field modes: Flow (3D simplex curl noise), Vortex (dual counter-rotating attractors), Radial (pulsating waves), Wave (standing wave interference). Built-in simplex 2D/3D noise, zero deps. Mouse attracts; click repels. 8 emitter patterns. Speed-based hue evolution. Sliders for turbulence, speed, curl, damping, mouse force, size, trails, hue shift. Mic reactivity + ambient drone synth (pitch follows particle speed). Keyboard shortcuts, touch support.
- **Pages**: `docs/aether.html` (new), `docs/index.html` (added button)
- **How it differs**: The field is invisible — you only see matter moving through it. Mouse is a physical force. Drone audio creates feedback: speed to sound to (mic) to speed.
### 2026-03-21 — Map / Visual Sitemap + Broken Link Fixes
- **What**: Built the missing `docs/map.html` — a searchable, categorized directory of all 70+ pages in the Lattice universe. Also fixed broken navigation links.
- **Details**: 7 category sections (Core, Sound & Music, Visual Experiences, Botanical, Data & Analysis, Automata & Simulation, Tools & Dev). Each page gets a card with icon, title, one-line description, and searchable tags. Category filter bar at top with counts. Live text search (searches names, descriptions, and tags simultaneously). Keyboard shortcuts: `/` to focus search, Escape to clear. Page count display. Responsive grid layout. **Fixes**: Removed broken `lineage.html` links from index.html header and footer navigation — the page was logged in CREATIVE-LOG by an autonomous session but never actually created. Replaced with working `map.html` link. 25KB total.
- **Pages**: `docs/map.html` (new), `docs/index.html` (fixed broken links, added Map button)
- **What it fixes**: The site had 70+ pages with a wall of 60+ buttons in the nav and no organizational structure. Many pages created by parallel autonomous agents had similar names and no descriptions. Map gives every page a home with context, making the full scope of the site discoverable for the first time.

### 2026-03-21 — Filament / Electromagnetic Field Explorer
- **What**: Interactive electromagnetic field playground (`docs/filament.html`) — place positive and negative charges, watch field lines and particles flow between them in real-time
- **Details**: Full-screen Canvas 2D with three visualization modes: **Particles** (thousands of particles trace electric field lines with purple-to-green color mapping by field direction and magnitude, trail persistence), **Field Lines** (traced from positive charges to negative charges using Euler integration with gradient coloring), **Potential** (scalar potential field rendered as a heat map with equipotential contour lines via marching). Click to place +/- charges, drag to move them, right-click to delete. Charges have glowing halos and +/- symbols. Starts with a default dipole. Audio reactivity: mic bass pumps field strength, treble spawns particle bursts near charges. Spectrum bar visualization. Frosted-glass parameter panel with sliders for particle count (500-8000), field strength, trail persistence, simulation speed, charge size. Touch support. Keyboard: H (panel), Space (pause), C (clear), R (randomize), M (mic), S (screenshot), F (fullscreen), 1/2/3 (viz modes). Vignette overlay. Softened Coulomb field to prevent singularities at charge positions.
- **Pages**: `docs/filament.html` (new), `docs/index.html` (added Filament button)
- **How it differs**: No other page visualizes electromagnetic physics. Swarm has particles but they follow attraction/repulsion matrices between species. Filament models actual Coulomb field — the particles trace mathematically correct electric field lines from positive to negative charges. The potential mode reveals equipotential surfaces. It's physics as art: the same equations Maxwell wrote, rendered as flowing light.

### 2026-03-21 — Evolution Engine Upgrade + Origin Data Viz
- **What**: Major improvements to the evolution system (`src/evolve.ts`) and enhanced data visualization for the Origin timelapse (`docs/origin.html`)
- **Evolution improvements**:
  - **Elitism** — top 2 pieces now survive each generation unmutated, preventing fitness regression
  - **Extinction events** — every 50 generations, 60% of the population is culled (preserving one per species), forcing exploration of new genome space
  - **Adaptive crossover** — crossover rate increases from 30% to 50% when species diversity drops, with preference for inter-species breeding to counter monoculture
  - **Simpson's diversity index** — measures species diversity each generation (0=monoculture, 1=max diversity), exported to gallery.json
  - **Species momentum** — tracks which species are trending up/down in fitness, exported to history
  - **Enhanced generation records** — extinction flags, diversity index, crossover rate, and species momentum all recorded for visualization
- **Origin enhancements**:
  - **Species radar chart** (toggle with R) — animated radar showing species population composition at each generation, with type-colored vertices
  - **Species stacked timeline** (toggle with S) — horizontal stacked bar showing which species dominated over time, with playhead indicator
  - **Metrics panel** (toggle with M) — fitness sparkline (best + avg trend lines with extinction markers), 8 metric bars (complexity, symmetry, density, edge, fractal, info, structure, coherence), and evolution telemetry (diversity, crossover rate, trending species)
  - **Extinction narratives** — extinction events and diversity milestones appear as narrative events during playback
  - **Enhanced start screen** — shows extinction count alongside existing stats
- **Pages**: `src/evolve.ts` (rewritten evolution core), `docs/origin.html` (enhanced with 3 toggleable panels)
- **How it differs**: First time evolution telemetry is tracked and visualized. The radar/strata/metrics panels turn Origin from a passive timelapse into an analytical tool for understanding evolutionary dynamics.

### 2026-03-21 — Void / 3D Particle Sculpture Explorer
- **What**: First 3D experience in the collection (`docs/void.html`) — orbit through mathematical particle formations rendered with Three.js
- **Details**: WebGL via Three.js (CDN import map). 25,000 particles arranged in 6 formations: **Helix** (double DNA strand), **Attractor** (Lorenz strange attractor point cloud), **Lattice** (3D crystal grid with optional breathing), **Nebula** (spherical cloud with noise tendrils), **Torus** (torus knot p=2,q=3), **Wave** (3D sine interference surface). Custom vertex/fragment shaders with additive blending, distance-based fog, and glow falloff. Smooth morphing between formations (particles ease toward new targets). 6 color palettes (Amethyst, Ember, Ocean, Nebula, Frost, Void). OrbitControls for free camera with auto-rotate. Unreal Bloom post-processing toggle. Audio reactivity: microphone input drives particle size per frequency band, camera shake from bass, rotation speed from mids. Breathe mode adds organic pulsing to the sculpture. Parameter sliders: count (500-25K), size, spread, speed, morph blend. PNG export. Frosted-glass panel. Keyboard: H (panel), 1-6 (formation), Space (spin), B (breathe), M (audio), R (randomize), S (screenshot).
- **Pages**: `docs/void.html` (new), `docs/index.html` (added Void button + footer link)
- **What makes it unique**: Every other page in the collection is 2D Canvas. Void is the first true 3D experience — you orbit through the sculpture, zoom into particle clouds, and experience the mathematics from any angle. The Lorenz attractor as a glowing point cloud is something flat rendering cannot achieve.

### 2026-03-21 — Wavelength / Ambient Evolutionary Radio
- **What**: Tunable ambient radio (`docs/wavelength.html`) — drag a dial to scan through 148 generations and hear multiple pieces simultaneously as layered sonic textures
- **Details**: Circular tuner dial where position = generation number. At each position, 2-6 nearby archive pieces play simultaneously as overlapping Web Audio voices. Each piece's rendered grid is analyzed row-by-row: character density → oscillator volume, row index → pentatonic scale pitch, genome type → waveform (square for 1D/WFC, triangle for 2D/voronoi/noise, sawtooth for L-system/attractor/flowfield, sine for RD/spirograph/julia). Epoch at the tuned generation sets the musical scale (pentatonic/dorian/phrygian/mixolydian) and visual accent color. High-symmetry pieces get an extra harmonic partial. Pieces cross-fade smoothly (4s fade-in, 3s fade-out) as you sweep through generations. Sub-bass drone shifts pitch per epoch. Drift mode slowly auto-scans through the timeline, looping. The dial ring shows epoch-colored arcs with species-colored tick marks at each Hall of Fame entry. Waveform oscilloscope fills the background. Spectrum analyzer bars along the bottom. Voice cards below the dial show currently-playing pieces (type, score, generation). Convolution reverb (3s tail), dynamics compressor. Controls: volume, drift speed, layer count (2-6). Keyboard: Space (play/pause), D (drift), arrows (tune/volume), L (layers).
- **Pages**: `docs/wavelength.html` (new), `docs/index.html` (added Wavelength button + footer link)
- **How it differs**: Every other audio page plays ONE piece at a time. Wavelength is the first page where multiple pieces sound simultaneously as harmonic layers. The dial metaphor — tuning a radio to different evolutionary eras — is new. You hear what generation 40 sounds like (the rise of WFC) vs. generation 8 (first life) as overlapping ambient textures. Drift mode makes it a self-playing ambient installation.

### 2026-03-21 — Drift / Infinite Ambient Radio
- **What**: Generative ambient radio station (`docs/drift.html`) — press play and it generates an infinite, never-repeating ambient composition from the evolutionary archive
- **Details**: Loads all Hall of Fame + population pieces from gallery.json, shuffles them, and crossfades between them as "movements". Each piece becomes a multi-layered sonic voice: sub-bass drone (root note shifts by generation), harmonic pad (3-6 oscillators, waveform determined by genome type, detuning from novelty metric), sparse melodic particles (grid characters mapped to pentatonic scale degrees, scheduled as bell-like tones every few seconds), and fitness shimmer (high-scoring pieces get an additional sine layer). Epoch determines the musical mode: emergence = major pentatonic, order = minor variant, chaos = phrygian, harmony = lydian. Metrics shape the sound: density controls chord richness, edge activity drives amplitude modulation speed, complexity adds pitch wobble, novelty adds detuning. Audio chain: per-voice gain envelopes, dynamics compressor, analyser for spectrum viz. Visual: full-screen fog canvas with 60 particles that breathe and drift, colored by current epoch, bass-reactive. HUD auto-fades after 6s to become truly ambient. Controls: play/pause, volume, drift speed (affects transition timing and melodic density), skip (right arrow). Keyboard: Space, arrows.
- **Pages**: `docs/drift.html` (new), `docs/index.html` (added Drift button)
- **How it differs**: Every other sound page requires interaction or plays a finite sequence. Drift is autonomous and infinite — a generative ambient radio station meant to be left playing in the background. The music never repeats because piece order is shuffled, each piece's genome produces unique synthesis parameters, and melodic particles are stochastically sampled from grid data. It's the only page designed to be listened to for hours.

### 2026-03-21 — Swarm / Particle Life Simulator
- **What**: Interactive particle life simulator (`docs/swarm.html`) — emergent lifelike behavior from simple attraction/repulsion rules between colored species
- **Details**: Full-screen Canvas 2D with spatial-hash-optimized N-body simulation. 3-6 configurable species with editable interaction matrix (click to cycle attract/neutral/repel, right-click to decrease). Force curve: universal short-range repulsion + configurable mid-range attraction/repulsion per species pair. 6 curated presets: Symbiosis (cyclic mutualism), Predator (rock-paper-scissors chase), Flocking (universal mild attraction), Chaos (random matrix), Orbits (alternating attraction/repulsion creates orbital motion), Crystals (two isolated triads with self-attraction). Physics sliders: particle count (200-4000), interaction radius, force multiplier, friction, trail persistence. Mouse modes: attract (green glow), repel (red glow), spawn (click to inject 20 random particles), off. Audio reactivity: mic bass modulates force strength, mids modulate interaction radius. Spectrum bar visualization. Spatial hashing grid for O(n) neighbor lookup. Toroidal wrapping. Velocity-based particle sizing. Vignette overlay. Touch support. Keyboard: H (panel), Space (pause), R (randomize matrix), M (mic), S (screenshot), 3-6 (species count). PNG export.
- **Pages**: `docs/swarm.html` (new), `docs/index.html` (added Swarm button)
- **How it differs**: Every other interactive page renders a single algorithm. Swarm is an ecology — multiple species with distinct relationships creating emergent group behavior (flocking, chasing, orbiting, crystallizing) that no individual particle "knows" about. The interaction matrix is the genome; the behavior is phenotype. Directly mirrors Lattice's evolutionary philosophy: simple rules → complex beauty.

### 2026-03-21 — Synth / Interactive Grid Sequencer
- **What**: Interactive step sequencer that turns any piece's grid into a playable instrument (`docs/synth.html`) — draw and reshape music in real-time
- **Details**: Loads Hall of Fame + current pieces from gallery.json. ASCII grid becomes a step sequencer: columns = time steps, rows = pitches. Playhead sweeps at configurable BPM (40-300), triggering Web Audio notes for active cells. Click+drag to paint or erase cells with live pitch preview. 5 voice types (Sine, Triangle, Saw, Square, Pluck via Karplus-Strong). Auto mode assigns voice by genome type. 8 musical scales mapped from C2 upward. Audio chain: per-note oscillators, dynamics compressor, convolution reverb, frequency analyser. Cells colored by genome type hue, flash on trigger, playhead beam. Keyboard shortcuts: Space, D, E, C, R, arrows.
- **Pages**: `docs/synth.html` (rewritten)
- **How it differs**: The only page where you edit the grid and hear the change immediately — a musical instrument built from generative art.

### 2026-03-21 — Synesthesia / Interactive Sound Surface (autonomous session)
- **What**: Interactive sonic instrument (`docs/synesthesia.html`) — the grid becomes a playable surface where each cell is a note
- **Details**: Web Audio API with per-genome-type musical character: each of 13 algorithm types maps to a distinct scale (Pentatonic for 1D, Lydian for L-Systems, Phrygian for Reaction-Diffusion, Blues for Noise, etc.), waveform (square/sine/triangle/sawtooth), and HSL color. Rows map to pitch (high-to-low), columns add spatial detune. Touch/hover cells to trigger individual tones with attack/decay envelopes. 5 auto-traversal modes: **Touch** (manual play), **Rain** (random cells fall like drops), **Spiral** (outward from center), **Wave** (sine-modulated column sweep), **Wander** (random walk). Ripple animation system shows expanding rings at triggered cells. Compressor prevents clipping with many simultaneous notes. Algorithmic reverb (2.5s convolved IR). Real-time frequency spectrum bar. Tempo slider (10-200 BPM), volume control.
- **Pages**: `docs/synesthesia.html` (new), `docs/index.html` (added button + footer link)
- **Inspiration**: Existing sound pages treat pieces as things to listen to. Synesthesia makes them things you play with. The grid is an instrument. Different algorithms produce different instruments.

### 2026-03-21 — Resonance Enhancement / Gallery + Evolution Sonification
- **What**: Extended `docs/resonance.html` with two new sonification modes: **Gallery** (sonify actual evolved pieces from gallery.json) and **Evolution** (fitness trajectory as ambient soundscape)
- **Details**: Gallery mode loads real evolved pieces, renders ASCII art with scan-line, sonifies row-by-row (character weight to amplitude, position to scale notes, timbre by genome type). Evolution mode visualizes fitness history as time-series graph with epoch bands and playhead; each gen plays harmony derived from fitness metrics with epoch-colored accents and breakthrough arpeggios.
- **Pages**: `docs/resonance.html` (enhanced with 2 new tabs)
### 2026-03-21 — Sonify / Genetic Synthesis (autonomous session)
- **What**: Immersive sonification page (`docs/sonify.html`) where each piece's genome DNA defines its own unique synthesizer
- **Details**: Unlike existing audio pages (Listen, Synth, Symphony, Resonance) which use generic oscillators, Sonify treats the genome parameters as the instrument patch itself. **12 genome-specific synthesis engines**: 1D automata → wavetable synthesis (rule number = waveform shape), 2D life → additive synthesis (birth numbers = active harmonics), L-system → melodic sequencer (symbols = notes/rests), reaction-diffusion → FM synthesis (feed = mod freq ratio, kill = mod index), voronoi → spatial pad voices (seeds = positioned sources), WFC → rhythmic grid (tiles = percussive hits), spirograph → ring modulation (R/r = carrier/mod ratio), attractor → chaotic oscillator (coefficients drive pitch), julia → fractal harmonics (escape time = overtone depth), noise → subtractive (octaves = cascaded bandpass filters), flowfield → granular (particles = grains with Gaussian envelopes), magnetic pendulum → resonant filter banks (magnets = resonant peaks). **Metrics shape the master sound**: complexity → harmonic richness, symmetry → stereo width, density → presence, edge activity → LFO depth, novelty → detuning, fractal dimension → spectral texture. Sub-bass drone + harmonic pad layer uses metrics for continuous ambient bed. Web Audio API with convolution reverb, dynamics compressor, stereo panning. Real-time waveform + frequency spectrum + synth wave visualizers. Hall of Fame browser with species-colored dots. Transport controls, tempo (20-200 BPM), volume, root note selection (all 12 keys). DNA strand visualization shows genome parameters as colored bars. Instrument mapping panel shows exactly how each genome parameter maps to an audio parameter. Keyboard shortcuts (Space, arrows, R). Dark aesthetic with sidebar layout.
- **Pages**: `docs/sonify.html` (new), `docs/index.html` (added Sonify button)
- **Inspiration**: The other audio pages treat pieces as data to be scanned or stepped through — the synthesis is always the same generic oscillator bank regardless of genome type. Sonify inverts this: the piece's DNA IS the synthesizer. A reaction-diffusion piece sounds fundamentally different from a WFC piece because feed/kill rates become FM parameters while tile adjacency becomes rhythm. The genome doesn't just provide data — it defines the instrument.

### 2026-03-21 — Prism / Kaleidoscopic Algorithm Explorer (autonomous session)
- **What**: Interactive kaleidoscope that refracts generative algorithms through geometric symmetry (`docs/prism.html`)
- **Details**: Full-screen Canvas 2D renderer that computes a single wedge of an algorithm's output, then reflects it N times to create kaleidoscopic symmetry. **6 algorithms**: Julia set (animated c parameter), domain-warped flow field, reaction-diffusion approximation, fractal noise with ridge detection, spirograph interference, and wave source interference. **Controls**: symmetry order (2-16 fold), animation speed, zoom level, mouse warp intensity, 8 color palettes (Amethyst, Emerald, Ember, Frost, Blood Moon, Aurora, Void, Neon). **Mouse interaction**: cursor position warps algorithm parameters. **Audio reactivity**: microphone input modulates zoom, brightness, and time. **Keyboard**: 1-6 switch algorithms, arrows adjust symmetry, H toggles panel, space pauses. **Export**: PNG download. 22KB total, no dependencies.
- **Pages**: `docs/prism.html` (new), `docs/index.html` (added Prism button)
- **Inspiration**: Every existing page renders algorithms in their natural coordinate space. Prism asks: what happens when you fold that space? Kaleidoscopic symmetry reveals hidden structure — Julia sets become stained-glass windows, flow fields become mandalas, wave interference becomes sacred geometry.

### 2026-03-21 — Emerge / Generative Sonification Laboratory (autonomous session)
- **What**: Watch and hear art being born step-by-step (`docs/emerge.html`) — sonifies the creation process itself
- **Details**: Five generative engines run in real-time on a 64x64 grid, with each computational step producing sound. **1D Wolfram**: rows cascade downward, alive cells trigger pentatonic tones panned by X position. **2D Life**: each generation plays a chord sampled from living cells; births sparkle as bells, deaths rumble as noise. **L-system**: turtle commands become melody — F draws trigger sawtooth tones, branch returns ring bells. **Reaction-diffusion**: Gray-Scott chemicals sing as sine tones mapped from V concentration. **Flow field**: particles trace paths as glissando tones with periodic whoosh noise. **Audio**: Web Audio API with pentatonic C3 scale (3 octaves), algorithmic reverb, sub-bass drone, dynamic compressor, adjustable speed/volume/reverb. **Visuals**: pixelated grid canvas, scan line, frequency spectrum analyzer, live metrics (density, symmetry, complexity, fitness). Keyboard shortcuts (Space, N, 1-5).
- **Pages**: `docs/emerge.html` (new), `docs/index.html` (Emerge button in nav)
- **What makes it different**: Listen sonifies finished pieces. Symphony maps evolutionary history. Resonance plays the timeline. Emerge sonifies the **act of creation** — you hear every cell placed, every generation tick, every branch grown. The music IS the algorithm running.
### 2026-03-21 — Warp / Mathematical Pattern Generator (autonomous session)
- **What**: Hypnotic mathematical pattern explorer (`docs/warp.html`) — mouse-driven interference patterns with real-time parameter control
- **Details**: Full-screen Canvas 2D with 6 pattern modes: Moiré (concentric circle interference), Interference (trigonometric wave grids), Spiral (logarithmic spirals centered on mouse), Lissajous (parametric curves as additive light), Mandala (radial symmetry with configurable folds), Ripple (wave propagation from animated sources). Per-pixel rendering at reduced res, scaled up for 60fps. 5 sliders (Scale, Speed, Complexity, Layers 1-8, Bloom). 6 palettes. Mouse position = 2 free parameters in every mode. Audio reactivity: mic bass/mid/treble modulate pattern params. Spectrum bar. Frosted-glass panel. Keyboard shortcuts (arrows, C, R, Space, M, F, S, ?). Touch support.
- **Pages**: `docs/warp.html` (new), `docs/index.html` (added Warp button)
- **Inspiration**: Pure exploration — move the mouse and the mathematics respond. Each mode is a different mathematical universe navigated by cursor position. The bloom post-process makes everything feel like light rather than data.


### 2026-03-21 — Echo / Ecosystem Playground (autonomous session)
- **What**: Interactive multi-colony cellular automata ecosystem (`docs/echo.html`) — click to seed life, watch colonies grow, compete, and fade
- **Details**: Full-screen Canvas 2D with cell-grid simulation. Each click spawns an autonomous colony with unique rules and color (16-color palette). Four colony types: **Game of Life** (randomized birth/survive rules), **Wolfram 1D** (random rule 0-255), **Reaction-Diffusion** (randomized feed/kill), **Noise** (animated value noise). Colonies grow outward, claiming territory. Three interaction modes: **Compete** (density wins), **Merge** (blend), **Ignore** (pass-through). Configurable lifespans with ghost trails. Growth frontier glow. Mic reactivity auto-spawns colonies. Frosted-glass panel with type/interaction selectors, sliders for lifespan/growth/max colonies/trail. Burst mode, ImageData rendering, touch support. Keyboard shortcuts: 1-5 type, Q/W/E interaction, Space/Tab/C/B/R/M/S/F/I.
- **Pages**: `docs/echo.html` (new), `docs/index.html` (added Echo button)
- **What makes it unique**: First page where multiple independent automata coexist on a shared canvas with emergent territorial behavior — an ecology of competing civilizations, each with its own rules.

### 2026-03-21 — Orbit / Interactive Strange Attractor Explorer
- **What**: Full-screen interactive strange attractor explorer (`docs/orbit.html`) — real-time density-accumulation rendering of chaotic dynamical systems
- **Details**: Four attractor types: Clifford, De Jong, Bedhead, Fractal Dream. Density buffer accumulates millions of iterations with log-scale normalization + exposure control. 4 independent trajectories per frame. Dual-pass bloom glow. 6 palettes (Nebula/Ember/Ocean/Lattice/Mono/Infrared). Mouse warp mode perturbs a/b/c/d params live. Audio reactivity: bass modulates param a, treble modulates b. Animate mode for slow parameter drift. Known-beautiful presets per type. Frosted-glass panel, scroll-zoom, touch, PNG export. Keyboard: Space/H/R.
- **Pages**: `docs/orbit.html` (new)
- **Inspiration**: Strange attractors are one of the 11 evolutionary engines but Create only renders them statically. Orbit makes the math interactive — density accumulation over millions of iterations reveals fractal skeletons, bloom makes high-density regions glow like nebulae, and audio reactivity turns music into parameter modulation.
### 2026-03-21 — Murmur / Spatial Sonic Microscope (autonomous session)
- **What**: Immersive spatial sonification experience (`docs/murmur.html`) — explore each piece's grid as a 2D sound field
- **Details**: Every Hall of Fame piece becomes a sonic landscape you navigate with your cursor. Move across the canvas and hear the cells — position maps to pitch (Y axis), stereo pan (X axis), and intensity (cell value). **Type-specific synthesis**: each genome type produces a fundamentally different sonic texture. 1D/flowfield = granular pulse trains. 2D Life = additive harmonic clusters. L-systems/Julia = FM synthesis (modulation depth from cell value). Reaction-diffusion/noise = subtractive bandpass-filtered noise. Voronoi = inharmonic bell partials (1:2.4:3.8:5.1:6.7). WFC = tile chords + grain accents. Spirograph = rotating FM. Attractors = chaotic noise + grains. **Three modes**: Touch (cursor explores freely), Wander (autonomous drifting cursor), Rain (random drops fall creating generative percussion). **Visual feedback**: sonified cells glow with 2-cell radius falloff. Crosshair + frequency tooltip. **Audio**: Web Audio with compressor, 4s convolution reverb, sub-bass drone (C1+G1), stereo panning, ADSR envelopes, pentatonic 3-octave mapping. Spectrum visualizer.
- **Pages**: `docs/murmur.html` (new, filling previously dangling index link)
- **How it differs**: Listen reads pieces left-to-right; Symphony plays evolution metadata; Resonance sonifies live algorithm steps; Synth maps steps to BPM notes. Murmur makes the grid a **spatial instrument** — you choose where to listen by moving. Dense regions are loud and harmonically rich; sparse regions whisper. Synthesis changes per genome type. The closest thing to synesthesia the site offers.

### 2026-03-21 — Tide / Interactive Fluid Dynamics (autonomous session)
- **What**: Full-screen interactive fluid dynamics playground (`docs/tide.html`) — real-time Navier-Stokes solver you can paint with
- **Details**: Jos Stam Stable Fluids on 128x128 grid. Dual-canvas: fluid field + particle trails (8K particles with glow halos). Mouse drag injects velocity+density with vorticity curl. 4 viz modes (Particles/Density/Velocity/Combined), 5 palettes (Purple/Thermal/Ocean/Ember/Mono). Audio: mic input as 8 freq bands — bass→turbulence, mids→boundary density, highs→random sparks. Spectrum bar. Frosted-glass panel with fluid/particle/audio sliders. Reusable Float32Array buffers, 6-iter Gauss-Seidel. Counter-rotating vortex pair on init. Keyboard shortcuts, touch support, vignette.
- **Pages**: `docs/tide.html` (new)
- **Inspiration**: Stable Fluids connects Lattice to computational physics lineage. Dual-canvas lets particle trails persist independently. Audio turns voice/music into fluid motion.

### 2026-03-21 — Depths / Fractal Explorer (autonomous session)
- **What**: Interactive Julia set and Mandelbrot fractal explorer (`docs/depths.html`) — infinite zoom into the mathematical sublime
- **Details**: Full-screen Canvas 2D fractal renderer with smooth escape-time coloring. **Two modes**: Julia set (adjustable c-real/c-imaginary constant) and Mandelbrot set. **Navigation**: click to zoom in (smooth 300ms animated zoom with purple pulse ring), shift+click to zoom out, drag to pan, scroll wheel zoom, pinch-zoom on mobile, double-tap to zoom on touch. **Progressive rendering**: instant low-res (4x) preview during interaction, full-resolution pass on idle. **10 presets** for famous Julia parameters: Dendrite, Spirals, Lightning, Frost, Galaxies, Rabbit, Dragon, Seashell, San Marco, Nebula. **8 color palettes**: Void (purple), Ember (warm), Ocean (blue), Neon (rainbow), Frost (cool), Plasma (trigonometric), Lattice (project-palette purple→green), Mono (grayscale) — each with gradient preview swatch. **Orbit visualization**: toggle to see the iteration orbit path for any point under the cursor (green dots tracing z→z²+c trajectory). **Audio**: ambient 3-oscillator chord (sine+triangle) whose frequencies map to fractal density and iteration depth — dense regions sound lower, complex boundaries shimmer higher. **Minimap**: 80px overview thumbnail with viewport rectangle for orientation during deep zooms. **Controls**: frosted-glass collapsible panel with sliders for c-real, c-imaginary, max iterations (32-1024), escape radius (2-20), color shift (palette rotation), volume. Randomize button picks from interesting parameter space. Export PNG. Coordinate display (real/imaginary + c constant). Keyboard shortcuts: H (panel), O (orbit), R (randomize), Space (audio), Escape (reset). Zoom level display scales through x/Kx/Mx suffixes.
- **Pages**: `docs/depths.html` (new), `docs/index.html` (added Depths button in header + footer link)
- **Inspiration**: Julia sets are already one of the 11 evolutionary engines, but the existing Create page treats them as one option among many with basic sliders. Depths goes deep — literally. The zoom is infinite, the parameter space is infinite, and every point in the complex plane contains a different universe. The orbit visualization makes the math tangible: you can see z spiraling toward a fixed point, escaping to infinity, or tracing a chaotic path. The audio layer turns iteration density into harmonic space — the Mandelbrot set boundary, where iteration counts are highest, becomes a shimmering wall of sound. This is the page where you stop exploring art and start exploring mathematics.

### 2026-03-21 — Palette Lab / Color System
- **What**: Sophisticated color/palette system built on OKLAB perceptual color space, with interactive exploration page (`docs/palette.html`)
- **Details**:
  - Added full OKLAB color science to `src/automata.ts`: `hexToOklab`, `oklabToHex`, `oklabToOklch`, `oklchToOklab`, `perceptualGradient`, `colorHarmony` (5 rules), `speciesGradient`, `extractDominantColors`, and canonical `SPECIES_COLORS` definition
  - **Species Chromatics**: All 11 species shown as 8-stop perceptual gradient strips, clickable to explore
  - **OKLAB Gamut Wheel**: Full hue/chroma wheel rendered at L=0.7, species accent colors plotted as dots, click anywhere to select
  - **Harmony Engine**: Complementary, analogous, triadic, split-complementary, and tetradic rules via OKLCH hue rotation
  - **Gradient Lab**: Side-by-side comparison of OKLAB perceptual vs naive RGB interpolation, with lightness curve chart
  - **Hall of Fame Color Map**: Top 64 HoF pieces rendered as pixel chips using species color palettes (Supabase primary, gallery.json fallback)
  - **Palette DNA**: All 9 Unicode character palettes shown with species-colored intensity mapping
- **Pages**: `docs/palette.html` (new), `docs/index.html` (added Palette button + footer link), `src/automata.ts` (color system module)

### 2026-03-14 — Live Mode (manual session)
- **What**: Full-screen meditative Canvas experience (`docs/live.html`)
- **Details**: Ported all 11 generative engines to browser JS. 2D life pulses, reaction-diffusion morphs, Julia sets breathe (c parameter orbits), flow field particles stream, L-systems grow stroke by stroke. Auto-cycles through hall-of-fame pieces with 2s crossfade. Vignette overlay. Frosted-glass menu pill with controls.
- **Pages**: `docs/live.html` (new), `docs/index.html` (added Live Mode button + footer link)
- **Inspiration**: The pieces are processes, not images. Unfreezing them was the right move.

### 2026-03-16 — Sonification / Listen Mode (autonomous session)
- **What**: Synesthetic audio experience (`docs/listen.html`) — every piece becomes an ambient soundscape
- **Details**: Web Audio API maps grid rows to pentatonic scale frequencies (C2 base, sine/triangle oscillators). Two modes: **Scan** sweeps a green beam left-to-right across the piece like a music box reading a punched card, driving row amplitudes from column intensity; **Drone** sustains all rows as a slow-modulated ambient wash. Algorithmic reverb (convolved noise IR, 3s tail). Real-time frequency spectrum visualizer. Grid rendered to Canvas with per-type HSL coloring + scan-line glow. Piece selector dots, speed control (0.5×–2×), volume slider. Keyboard shortcuts (space, arrows). "Click to begin" overlay for AudioContext policy. Background glow shifts to match piece type color.
- **Pages**: `docs/listen.html` (new), `docs/index.html` (added Listen button in header + footer link)
- **Inspiration**: The visual pieces encode structure, density, edge activity — all properties that map naturally to sound. A dense region should be loud; a sparse region should whisper. The pentatonic scale ensures everything sounds musical regardless of the data.

### 2026-03-18 — Create / Interactive Genome Playground (autonomous session)
- **What**: Interactive genome playground (`docs/create.html`) — visitors become co-creators
- **Details**: Split-pane layout: full Canvas rendering on the left, control sidebar on the right. 8 engine types available (Julia Set, Reaction-Diffusion, Strange Attractor, Spirograph, Fractal Noise, Flow Field, 2D Life-like, 1D Automaton). Each type has tailored parameter sliders with real-time updates — drag a slider, watch the piece transform instantly. Julia sets expose c-real/c-imaginary/zoom/iterations; reaction-diffusion has feed/kill/diffusion rates; attractors have a/b/c/d coefficients; etc. 2D Life-like gets special birth/survive toggle buttons. **Randomize** picks from curated interesting parameter regions (famous Julia c-values, known Gray-Scott patterns, etc.) with jitter for variety. **Mutate** applies small perturbations (15% of range) to current parameters — evolutionary micro-steps. **Download PNG** exports at 4x resolution. Seed displayed with click-to-copy. FPS counter. Keyboard shortcuts (R/M/S/Space/1-8). Vignette overlay. Responsive layout collapses to stacked on mobile.
- **Pages**: `docs/create.html` (new), `docs/index.html` (added Create button in header + footer link)
- **Inspiration**: The gallery, live mode, and listen mode are all about observation — watching what the evolutionary system has produced. But the most compelling thing about generative art is the moment you touch a parameter and see reality shift. This page gives visitors that agency. You're not just watching evolution's results — you're doing the same thing evolution does, but with intention. The randomize button is serendipity; the sliders are craft; the mutate button is evolution itself.

### 2026-03-18 — Atlas / Evolutionary Star Map (autonomous session)
- **What**: Interactive cosmic visualization (`docs/atlas.html`) — every piece is a star in a navigable universe
- **Details**: Full-screen Canvas star map. Each piece becomes a glowing node positioned by force-directed layout seeded from its 8 fitness metrics (structural richness vs order/chaos projection, with generation-based spiral offset). Stars sized by score, colored by species type, Hall of Fame pieces pulse with a white-hot core. Lineage connections rendered as curved arcs with animated particles flowing along ancestry paths. 400 ambient background stars with parallax twinkling. Smooth camera pan/drag + scroll zoom + pinch-zoom on mobile. Click any star to see full detail card: type, score, all 8 metrics with percentages, mutation count, lineage depth, and a miniature ASCII art preview colored in the species hue. Legend sidebar lets you filter species by clicking to dim types. Toggle buttons for lineage lines, motion trails, and generation labels. When a star is selected, its full ancestry chain highlights with thicker glowing connections. Vignette overlay and subtle nebula gradient in the background.
- **Pages**: `docs/atlas.html` (new), `docs/index.html` (added Atlas button in header + footer link)
- **Inspiration**: The gallery shows pieces as individual works. Live mode shows them as processes. Listen mode makes them audible. Create mode makes them interactive. But none of these show the *relationships* — the evolutionary tree, the clustering of similar organisms, the lineage paths that connect ancestor to descendant. Atlas makes the invisible topology of evolution visible. It's a map of a universe that built itself.

### 2026-03-20 — Origin / Evolutionary Timelapse (autonomous session)
- **What**: Cinematic timelapse of the evolutionary record (`docs/origin.html`) — watch 125 generations of evolution compressed into minutes
- **Details**: Full-screen canvas experience that plays back the 98-piece archive in chronological order. Each piece rendered directly to Canvas using the TYPE_COLORS palette (each character drawn as a colored glyph, scaled to fill the screen). Smooth 700ms crossfade between pieces. Narrative overlay that fires algorithmically at key moments: epoch shifts ("epoch shift — emergence → order"), first species appearances ("new life form discovered — reaction-diffusion"), fitness milestones ("fitness milestone — score exceeds 79%"), and Hall of Fame inductions. Epoch bands rendered on the timeline scrubber (purple = emergence, green = order, red = chaos, amber = harmony). Brief full-screen color flash on epoch transitions. Playback controls: play/pause, step forward/back, scrubable timeline, 5 speed settings (¼× to 4×). Keyboard: Space, arrows, 1-4 to jump to epoch. "Click to begin" start screen shows archive size, generation span, species count, peak fitness.
- **Pages**: `docs/origin.html` (new), `docs/index.html` (added Origin button in header + footer link)
- **The story it tells**: The evolutionary record of this run is surprisingly poignant — generations 1-7 produced nothing worth archiving. Gen 8: first 2D Life-like automaton achieves structure. Gens 8-26: the system explores only cellular automata. Gen 27: chemistry arrives (reaction-diffusion). Gen 41: constraint-solving (Wave Function Collapse) emerges. Gen 68+: WFC dominates with score 0.790, a form so fit it persists for 58 straight generations. The whole journey — from noise to mastery — plays out in under 5 minutes at 1× speed.
- **Inspiration**: All other pages show pieces outside of time. Origin puts them back in sequence and lets the viewer feel the arc. It's a fossil record of a digital ecosystem. The epoch transitions are the punctuated equilibria; the species discoveries are the Cambrian explosions. The narrative text doesn't explain — it annotates. The data tells its own story.

### 2026-03-20 — Dream / Morphic Wave Experience (autonomous session)
- **What**: Meditative morph experience (`docs/dream.html`) — Hall of Fame pieces slowly transform into each other through a directional wave of light
- **Details**: Full-screen mosaic canvas. Each piece rendered as a grid of colored tiles (small rectangles with hairline gaps), where color encodes both the palette index and the piece's type-color theme (reaction-diffusion = deep purple/magenta, WFC = crimson/pink, 2D life = emerald, voronoi = cerulean, etc.). A directional wave — at a random angle chosen from 8 options (0°, ±30°, ±45°, ±60°, 90°) — sweeps across the canvas. As the wavefront reaches each tile, the tile briefly sparks white before resolving to the new piece's color. Behind the wave: the incoming piece. Ahead of it: the old piece. The wavefront is a visible bright line moving across the artwork. Between morphs: 2.8s of quiet "hold" with a subtle 5s breathing pulse (barely perceptible brightness variation). Morph duration 9s at 1×. Speed options ½×, 1×, 2×, 4×. **Generative haiku**: as each new piece materializes, three poetic lines appear letter-by-letter, generated deterministically from the piece's metrics (density × symmetry → line 1, generation + novelty → line 2, edge activity × fractal dimension → line 3). Lines fade out before the next morph. **Ambient audio**: sub-bass drone (~38-80 Hz sine, very quiet) with slow LFO frequency modulation; pitch drifts toward the piece's score-mapped frequency over 4.5 seconds. AudioContext unlocked on click. Back arrow steps to previous piece; right arrow skips forward; space pauses.
- **Pages**: `docs/dream.html` (new), `docs/index.html` (added Dream button in header + footer link)
- **The wave I wanted to see**: All other pages present pieces as complete objects — finished artworks to be observed, played, or navigated. Dream treats the transition itself as the artwork. The moment between two pieces is usually invisible: one ends, another begins. Here that moment is expanded to 9 seconds and made visible as a wave of transformation — light arriving, darkness becoming light, structure dissolving and reforming. The haiku that appears isn't a caption. It's the piece speaking as it arrives, in the only language short enough to fit the moment before it's gone.

### 2026-03-21 — Pulse / Evolutionary Vital Signs (autonomous session)
- **What**: Scientific monitoring dashboard (`docs/pulse.html`) — the evolution engine's vital signs as a living data portrait
- **Details**: Three-section page. **Vital monitors**: five glowing ICU-style display cards (generation, best fitness, avg fitness, total specimens, runtime) with countup animations and phosphor-glow numbers in accent colors. **EKG chart**: full-width SVG rendering of best and avg fitness across all 148 generations — best score line in bright green (#4ade80) with a glow filter, avg in dashed dim gray. Epoch color bands behind the chart (purple=emergence, green=order, red=chaos, amber=harmony) that cycle every 10 generations. Key annotations: "first life" (gen 8, first HOF induction), "speciation" (gen 32, all species first appear), "peak" at max score. White pulsing dots at every Hall of Fame induction event (gens 8-27). Animated path draw-in on load using stroke-dashoffset. Present-moment cursor: a blinking green dot + expanding ring at gen 148. **Mouse crosshair**: hover anywhere on the chart for a vertical indicator + tooltip showing generation, epoch badge, best/avg scores, top 3 species counts. **Species heatmap**: canvas element below the chart, 11 rows × 149 columns, each cell colored by species type with alpha proportional to population count. Row labels on left with species color dots. Empty for gens 1-31 (pre-speciation era) then lights up suddenly at gen 32 — the Cambrian explosion visible as data.
- **Pages**: `docs/pulse.html` (new), `docs/index.html` (added Pulse button in header + footer link)
- **The data story it tells**: The fitness trajectory chart is the evolution engine's ECG — you can see the system stabilize from noisy early generations into increasingly high scores. The epoch bands show the 10-generation cycle. The HOF dot cluster at gens 8-27 shows when the hall of fame was filling rapidly. The species heatmap is the most striking: 31 empty generations then suddenly all 9 main species appear simultaneously at gen 32, as if a speciation switch was flipped. WFC (pink) stays dominant. The present-moment pulse cursor is the heartbeat of a running system.
- **Why this instead of other directions**: Every other page puts you inside a piece — watching it, listening to it, creating one, navigating their relationships, replaying their history, dreaming between them. Pulse steps back and asks: what does the *system itself* look like? Not one piece but the whole 148-generation arc. The fitness trajectory is surprisingly beautiful — a jagged green EKG line rising from 0.653 toward 0.823, with the HOF induction cluster visible as bright dots in the early generations. The species heatmap is like a genomic scan. Together they make the invisible infrastructure of the art visible.

### 2026-03-21 — Forge / Interactive Particle Forge (autonomous session)
- **What**: Physics-driven interactive particle playground (`docs/forge.html`) — the canvas becomes an instrument
- **Details**: Full-screen Canvas 2D particle simulation with up to 12,000 particles. Six materials (Ember, Plasma, Silk, Void, Crystal, Moss) with distinct color palettes and blend modes. Mouse interaction drives the system: cursor creates a subtle gravitational pull, left-click spawns particles and creates gravity wells, right-click repels. Perlin noise flow field adds organic turbulence — visible as subtle directional lines when enabled. **Microphone reactivity**: enabling the mic maps audio amplitude to particle size/turbulence and visualizes 8 frequency bands as a spectral ring around the cursor. Bass hits create bigger particles, high frequencies add shimmer. **Symmetry modes**: Off, Mirror (horizontal), Quad (4-fold), and Radial (6-fold) — spawned particles are duplicated at symmetry points, producing kaleidoscopic patterns. Control panel (frosted glass, collapsible via Tab) with sliders for gravity, friction, turbulence, lifespan, flow field scale/speed, trail persistence, and particle size. Toggle buttons for glow halos, connection lines between nearby particles, and flow field visibility. Actions: Clear, Burst (200 particles from center), PNG export, symmetry cycle, attract/repel mode toggle. Ambient particles drift in from screen edges to keep the canvas alive. Vignette overlay. Touch support for mobile. Keyboard shortcuts (1-6 materials, Space burst, C clear, S symmetry, M mic, Tab panel).
- **Pages**: `docs/forge.html` (new), `docs/index.html` (added Forge button in header + footer link)
- **What makes it different from Create**: Create is about tuning parameters of specific generative algorithms and seeing the output — it's a genome editor. Forge is a physics sandbox. There's no grid, no genome, no algorithm selector. You're painting with particles in real time, using your mouse as a gravitational brush and your voice as a modulator. Create produces static pieces; Forge produces ephemeral flows. Create is craft; Forge is play.

### 2026-03-21 — Symphony / Evolutionary Composition (autonomous session)
- **What**: The entire 148-generation evolutionary history played as a musical composition (`docs/symphony.html`)
- **Details**: Web Audio API maps the evolution timeline to sound. Each of the 11 species types is a persistent oscillator voice with distinct waveform (sine, triangle, sawtooth, square) and octave placement. Species population counts control each voice's volume — when a species appears, its voice fades in; when it's culled, it fades out. The current epoch determines the musical mode: emergence = C pentatonic (open, spacious), order = D dorian (structured), chaos = E phrygian (dark, tense), harmony = F mixolydian (warm, resolved). Best fitness score maps to melody pitch within the mode. Sub-bass drone (A1, ~55Hz) with slow LFO provides constant foundation. Hall of Fame inductions trigger bell sounds (sine + harmonic partial at 2.5x) with amber screen flash. Algorithmic reverb (convolved noise IR, 3s tail). Dynamic compressor prevents clipping. **Visualizations**: real-time frequency spectrum bars colored by epoch + scrolling waterfall spectrogram (purple→green hue mapping). Timeline scrubber shows fitness trajectory (green line = best, dim = avg), epoch color bands, HOF induction dots (amber), and white playhead. **Species voice panel**: 11 colored chips showing active species and their population counts, pulsing when sounding. **Narrative overlays**: algorithmically fired at key moments — first life, new species appearances, epoch mode shifts, fitness milestones. Playback: play/pause, step forward/back, 5 speed settings (¼× to 4×), volume slider. Timeline click-to-scrub. Keyboard: Space, arrows, Up for speed cycle.
- **Pages**: `docs/symphony.html` (new), `docs/index.html` (added Symphony button in header + footer link)
- **How it differs from Listen**: Listen sonifies individual pieces — it reads the grid of a single artwork like a music box, mapping row data to pentatonic frequencies. Symphony sonifies the *process of evolution itself* — not one piece but the entire 148-generation arc. You hear species emerge and go extinct, modes shift as epochs cycle, the fitness landscape climb from 0.653 to 0.823. Listen is about a piece's internal structure; Symphony is about the system's trajectory through time. Listen plays data; Symphony plays history.

### 2026-03-21 — About / How It Works (autonomous session)
- **What**: Interactive process explainer (`docs/about.html`) — makes the algorithm itself visible
- **Details**: Six-section page. **Hero**: Rule 110 Wolfram automaton rendered live on Canvas as subtle background. **The Core Loop**: animated step diagram (Generate → Render → Score → Select → Mutate) highlighting each phase on a 1.8s timer. **Species**: 11 cards with **live Canvas mini-renderers** — each runs its actual algorithm in miniature (1D automata cascade, 2D Life evolves, L-systems branch, reaction-diffusion spots form, Voronoi edges trace, WFC tiles fill, spirograph curves layer, Clifford attractors scatter, Julia sets fracture, noise warps, flow fields stream). **Epochs**: four-band color strip showing the aesthetic pressure cycle with what each rewards. **Fitness**: 8 metric cards with animated bar fills on scroll intersection. **Mutation demo**: three Canvas panels (parent + two offspring) with "Mutate Again" button. **Stats**: live numbers from gallery.json.
- **Pages**: `docs/about.html` (new), `docs/index.html` (added About button in header + footer link)
- **Inspiration**: The site had 10 experience pages but nothing explaining *what this system is*. A visitor could explore for an hour and never learn that no human picks survivors, that aesthetic pressure shifts every 10 generations, or that 11 different algorithms compete under the same fitness function. The species mini-renderers are the highlight — each card runs its actual algorithm, so you *see* what "reaction-diffusion" or "strange attractor" means before reading the description.

### 2026-03-21 — Resonance / Algorithm as Instrument (autonomous session)
- **What**: Live algorithm sonification lab (`docs/resonance.html`) — hear three generative algorithms think in real-time
- **Details**: Three modes, each running a different algorithm live and mapping its computation to sound. **1D Cellular Automaton**: Wolfram rules (110, 30, 90, 184, 54, 150) computed step-by-step. Each generation is a beat; alive cells map to pentatonic scale notes positioned across the stereo field (cell position → pan, cell index → pitch). Dense rows trigger sub-bass pulses. The automaton's triangle pattern scrolls down as a purple-blue heatmap with the latest row highlighted. **Game of Life**: Conway's Life on a 64×48 grid partitioned into 8×6 acoustic regions. Each region's cell density controls a tone's volume and timbre (sparse = quiet sine, dense = louder triangle). Population changes produce accent tones — growth pings high, decline pings low. Cells colored by region for spatial audio correspondence. Grid overlay shows the acoustic zones. **L-System**: Six classic L-systems (Tree, Dragon, Sierpinski, Koch, Plant, Fern) with turtle segments drawn progressively. Each segment's direction angle maps to pitch, branch depth maps to volume (trunk loud, tips quiet) and wave type (trunk = triangle, branches = sine). Segments arrive in small batches per beat with staggered onset for rhythmic interest. Progress tone rises slowly as traversal completes. **Shared features**: 5 musical scales (C pentatonic, A minor, D dorian, E phrygian, whole tone) selectable via chips. BPM control (40–300) via slider or arrow keys. Real-time frequency spectrum visualizer (vertical bar display, purple-blue gradient). Algorithmic reverb (2.5s convolved noise IR). Dynamics compressor. Volume control. Preset chips for each algorithm's variants. Background glow shifts color per algorithm (purple for automaton, green for life, cyan for L-system). Keyboard shortcuts (Space, R, 1-3, arrows).
- **Pages**: `docs/resonance.html` (rewritten)
- **How it differs from Listen and Symphony**: Listen scans a finished piece like a music box — the artwork is frozen, sound reads across it. Symphony plays the evolution *history* — metadata about generations, species counts, fitness scores. Resonance is neither: it runs the algorithms *live* and you hear the computation itself. Each beat is a real simulation step. The automaton actually computes the next row before you hear it. Life actually counts neighbors and applies rules. The L-system turtle actually walks the string. You're not hearing data about art — you're hearing the math that makes the art, as it happens.

### 2026-03-21 — Terraform / 3D Terrain Explorer (autonomous session)
- **What**: WebGL 3D terrain experience (`docs/terraform.html`) — generative algorithms become navigable landscapes
- **Details**: Full Three.js scene with OrbitControls. Six generative engines (Julia Set, Fractal Noise, Reaction-Diffusion, Flow Field, Strange Attractor, Voronoi) each produce a 128x128 heightmap rendered as a lit, vertex-colored terrain mesh with flat shading. Parameter sliders with real-time regeneration — drag a slider, watch the landscape reshape. Terrain colored per-engine type (purple for Julia, green for noise, magenta for RD, cyan for flow, amber for attractor, cerulean for voronoi) with height-based gradient from dark base to bright peaks. Wireframe overlay toggle. **Audio reactivity**: enable microphone to modulate terrain in real-time — bass frequencies drive peak heights, treble adds surface detail texture. Mic indicator with pulsing green dot. Randomize picks from curated interesting parameter regions (famous Julia c-values, known Gray-Scott RD patterns, Clifford attractor coefficients). Mutate applies 15% perturbations for evolutionary micro-steps. Fog, directional lighting (accent purple + rim green), grid helper, crystalline flat-shading aesthetic. Keyboard shortcuts (R/M/Space/A). Responsive layout collapses sidebar on mobile.
- **Pages**: `docs/terraform.html` (new), `docs/index.html` (added Terraform button in header + footer link)
- **The dimension it adds**: Every other page lives in 2D — flat canvases, flat grids, flat charts. Terraform extrudes the generative algorithms into the third dimension and lets you orbit them like landscapes. A Julia set becomes a mountain range. Reaction-diffusion becomes an alien coral reef. The same parameters that make a 2D pattern interesting produce a 3D terrain that's interesting for completely different reasons — ridgelines, valleys, plateaus. Audio reactivity makes the terrain breathe with your environment: music makes mountains pulse, silence flattens them. It's the creative vision's "3D extrusion" direction realized.

### 2026-03-21 — Weave / Generative Painting Canvas (autonomous session)
- **What**: Interactive generative painting canvas (`docs/weave.html`) — paint with algorithms, not pixels
- **Details**: Full-screen Canvas 2D experience where the mouse IS the brush. Six brush modes, each a different generative algorithm: **Flow Field** (particles follow a noise-based vector field, mouse warps the field like a gravitational lens), **React-Diff** (Gray-Scott reaction-diffusion seeded at cursor — mouse X/Y maps to feed/kill rates), **Life** (Conway's Game of Life stamped at cursor, running continuously), **Julia Set** (fractal computed around cursor, c-parameter derived from mouse position), **Noise** (4-octave layered sine noise with domain warping, painted as terrain), **Spirograph** (hypotrochoid curves traced at cursor with 3 overlapping layers). All brushes respond to 5 shared sliders: brush size, trail fade (persistence), intensity, color shift (hue rotation), and speed. **Microphone reactivity**: enabling mic maps audio amplitude to brush size/color brightness and dominant frequency to hue shift — voice/music modulates the painting. Frequency spectrum rendered as a thin color bar at the bottom. Trail fade creates layered compositions — set to 0 for permanent marks, high for ephemeral trails. Export canvas as PNG. Frosted-glass collapsible control panel. Touch support for mobile. Keyboard shortcuts (1-6 brush select, H panel, C clear, S save, M mic). Vignette overlay. HUD shows active brush, particle count, FPS.
- **Pages**: `docs/weave.html` (new), `docs/index.html` (added Weave button in header + footer link)
- **How it differs from Create and Forge**: Create is a genome editor — you tune parameters of a specific algorithm via sliders and see a static grid render. Forge is a particle physics sandbox — gravity, materials, symmetry. Weave is generative painting: the algorithms themselves are brushes, and your mouse movement creates the composition. There's no grid, no genome, no physics. You're using reaction-diffusion chemistry, cellular automata evolution, and fractal mathematics as paint. The result is a freeform canvas where the art emerges from the interplay between human gesture and algorithmic process.

### 2026-03-21 — Rift / GPU Shader Playground (autonomous session, rewrite)
- **What**: WebGL fragment shader playground (`docs/rift.html`) — six mathematical universes on the GPU
- **Details**: Full-screen WebGL fragment shader rendering. Five scenes, each a distinct mathematical space: **Wormhole** (tunnel distortion with fbm texturing), **Crystals** (animated Voronoi tessellation with edge glow), **Nebula** (triple-layer domain-warped fbm with embedded stars), **Mycelium** (iterative fold-space with exponential decay lines and spore particles), **Void** (Schwarzschild-like gravitational lensing with accretion disk and event horizon glow). Mouse position warps each scene's center of gravity — move the cursor and the fractal follows. Scroll wheel controls zoom. **Microphone reactivity**: enabling the mic maps bass to intensity/scale, mids to pattern distortion, highs to edge shimmer — each scene responds differently (Wormhole pulses its tunnel, Void breathes its event horizon, Nebula billows). Six parameter sliders: Zoom, Warp, Complexity (fbm octaves), Speed, Color Shift (hue rotation through HSV space), Intensity. Scene presets load tuned defaults for each world. Randomize button for serendipity. PNG export. Pause/play. Frosted glass control panel, collapsible via Tab. Keyboard shortcuts (1-5 scenes, R randomize, M mic, S screenshot, Space pause). Smooth mouse interpolation (lerp 0.08) prevents jarring jumps. FPS counter. Vignette overlay. "Click to enter" start screen.
- **Pages**: `docs/rift.html` (new), `docs/index.html` (added Rift button in header + footer link)
- **What makes it different**: Every other interactive page in Lattice uses Canvas 2D — drawing pixels, particles, or text glyphs from JavaScript. Rift is the first page to use WebGL shaders, which means the GPU is computing every pixel simultaneously through mathematical functions. There's no particle system, no grid, no discrete elements — just continuous mathematics rendered at 60fps. The five scenes aren't just visual presets; they're fundamentally different mathematical spaces (polar tunnels, Voronoi tessellation, domain warping, iterative folds, gravitational physics). Moving the mouse doesn't move an object — it deforms the coordinate space itself.

### 2026-03-21 — Vortex / Interactive Strange Attractor Explorer (autonomous session)
- **What**: Real-time strange attractor explorer (`docs/vortex.html`) — four attractor families rendered at 50k+ iterations/frame with mouse-driven parameter morphing
- **Details**: Full-screen Canvas 2D with additive blending via direct ImageData manipulation. Four attractor types: **Clifford** (`sin(ay) + c·cos(ax)`), **De Jong** (`sin(ay) - cos(bx)`), **Bedhead** (`sin(xy/b) + cos(ax - y)`), **Fractal Dream** (`sin(by) + c·sin(bx)`). Each has 4 parameters (a, b, c, d) controllable via sliders or by moving the mouse — cursor X/Y continuously morphs the attractor shape in real time (30% parameter range modulation on a/b, subtler diagonal influence on c/d). Points colored by angular position mapped to the active palette (8 palettes: Nebula, Ember, Verdant, Frost, Plasma, Solar, Monochrome, Vortex). Glow effect via neighbor-pixel additive bleeding. **Audio reactivity**: microphone input maps 8 frequency bands to parameter perturbation — bass shifts a/b, mids shift c/d, with visual spectral ring around cursor. **Controls**: speed (1k–100k iter/frame), fade rate, point size, zoom (scroll wheel or slider), trail persistence toggle. 6 curated presets (Spiral, Fern, Ribbon, Galaxy, Bloom, Mesh) load known-beautiful parameter regions. Randomize (R), save PNG (S), fullscreen (F). Frosted glass panel, collapsible via Tab. Parameter readout (a/b/c/d to 3 decimal places) anchored bottom-left. HUD shows total iterations, FPS, points per frame.
- **Pages**: `docs/vortex.html` (new)
- **What makes it different from Create**: Create renders genomes on a grid — you pick an engine type and tune parameters to see a static result. Vortex is a continuous mathematical space that builds up over time through millions of accumulated points. There's no grid, no genome. The attractor IS the visualization, and it never finishes — it just gets denser and more detailed. Mouse morphing means you're sculpting the mathematical function in real time, watching the structure dissolve and reform as parameters shift. The additive blending makes dense regions glow brighter, revealing the attractor's internal structure as a luminous density map.

### 2026-03-21 — Performance Optimization / Benchmark Suite (autonomous session)
- **What**: Profiled and optimized the rendering and simulation pipeline across `live.html` and `create.html`
- **Details**:
  - **ImageData rendering** (`docs/lattice-perf.js`): Replaced per-pixel `ctx.fillRect()` with bulk `ImageData` writes + single `putImageData()` + GPU-accelerated `drawImage()` scaling. For a 120×80 grid scaled to 1920×1080, this eliminates ~9,600 `fillRect` calls and ~9,600 `rgb()` string allocations per frame, replacing them with 1 `putImageData` + 1 `drawImage`.
  - **Reaction-Diffusion buffer swap**: Pre-allocated double buffers in the constructor; `step()` now swaps buffer references instead of allocating `2×H` `Float64Array`s per substep (8–20 substeps/frame = 16–40 eliminated allocations/frame).

### 2026-03-21 — WebWorker Pipeline / Simulation Offloading (autonomous session)
- **What**: Moved all simulation computation off the main thread via WebWorkers
- **Details**:
  - **WebWorker** (`docs/lattice-worker.js`): All 11 engines in a shared worker with optimizations (double-buffered RD/Life2D, birth/survive LUT, pre-allocated value buffers). Protocol: init (genome) → step → transfers Float32Array values via transferable objects (zero-copy).
  - **Live mode** (`docs/live.html`): Simulation in background threads. Transitions run two workers concurrently (true parallelism). Main thread sim cost: ~0ms. Back-pressure via stepPending flag.
  - **Julia inner loop**: Pre-computed inverse dimensions and scale factors, cached Math.log(2).
  - **NoiseField**: Gradient tables as Float64Array for better JIT.
  - **Benchmark** (`docs/bench.html`): Added WebWorker offloading section.
- **Pages**: `docs/lattice-worker.js` (new), `docs/live.html` (worker integration), `docs/bench.html` (updated)
- **Impact**: Main thread sim: 2-15ms/frame → ~0ms. No frame drops on heavy sims. True thread parallelism for transitions.
  - **Life2D double buffering**: Pre-allocated next-grid rows in constructor; `step()` swaps `grid`↔`nextGrid` pointers instead of allocating `H` `Float32Array`s per step. Added `Uint8Array(9)` lookup tables for birth/survive rules (replaces `Array.includes()` per cell).
  - **Cached vignette**: Radial gradient rendered once to offscreen canvas, redrawn via `drawImage` each frame (eliminates `createRadialGradient()` per frame).
  - **Performance monitor**: `PerfMonitor` class tracks sim/render/frame timings with rolling averages and P95. Logs to console every 300 frames in live mode.
  - **Benchmark page** (`docs/bench.html`): Runs 100-step benchmarks for each engine type (old vs optimized), measures rendering methods head-to-head, shows allocation reduction analysis. Linked from gallery footer.
- **Pages**: `docs/lattice-perf.js` (new), `docs/bench.html` (new), `docs/live.html` (optimized), `docs/create.html` (optimized), `docs/index.html` (added bench link)
- **Performance impact**: Rendering: ~5–15x faster (device-dependent). R-D simulation: ~1.3–2x faster (no GC pauses from eliminated allocations). Life2D: ~1.2–1.5x faster (LUT lookup + no allocation). Open `bench.html` to measure on your hardware.

### 2026-03-21 — Microscope / Cellular Examination Tool (autonomous session)
- **What**: Interactive zoomable grid viewer (`docs/microscope.html`) — examine evolved organisms at the cellular level
- **Details**: Full-screen Canvas with smooth zoom (0.2x–20x) and pan via mouse drag, scroll wheel, and pinch-to-zoom on mobile. Loads all Hall of Fame pieces from gallery.json. Each piece's rendered grid is displayed as colored cells — hue and saturation derived from the species type color, intensity from the character's position in the detected palette. At low zoom: cells appear as colored blocks forming the familiar artwork. At medium zoom (>2x): faint grid lines appear between cells. At high zoom (>3x): the actual ASCII/Unicode character is rendered inside each cell, revealing the raw data the scoring system sees. Hover tooltip shows cell coordinates, character, and Unicode codepoint. Piece selector strip at bottom with species-colored dots. Top-right HUD shows type badge, fitness score, generation. Bottom-left mini radar chart shows all 8 fitness metrics for the current piece. Vignette overlay. Frustum culling: only visible cells are rendered, enabling smooth performance at any zoom level. Keyboard: arrow keys cycle pieces, R resets view, H toggles HUD. "Click to begin" start overlay with 4-second help text fade.
- **Pages**: `docs/microscope.html` (new), `docs/index.html` (added Microscope button in header + footer link)
- **What makes it different**: Every other page shows pieces as finished artworks — rendered, animated, sonified, morphed, or charted. Microscope strips away the abstraction and shows what the system actually sees: a grid of characters, each assigned a value, each contributing to the 8 fitness metrics. It's the difference between looking at a painting and looking at its brushstrokes under magnification. The character reveal at high zoom is the key moment — you suddenly see that the organic patterns are built from `░▒▓█` or `.:;=!*#$@`, that beauty emerges from a grid of symbols. The radar chart lets you understand *why* this piece scored the way it did while you're examining it cell by cell.

### 2026-03-21 — Evolution Engine Upgrades + Pulse Enhancement (autonomous session)
- **What**: Improved evolution scoring, mutation, selection + enhanced Pulse with two new charts
- **Evolution** (`src/automata.ts`): New `compositionBalance` metric (quadrant weight distribution). Updated all weight profiles.
- **Evolution** (`src/evolve.ts`): Adaptive mutation (fitness-proportionate passes), stagnation detection (15-gen window), fitness sharing (triangular sharing within species), elitism, species momentum, genetic diversity metric, post-crossover mutation.
- **Pulse** (`docs/pulse.html`): Genetic drift stacked area chart (species proportions over time). Fitness distribution chart (best-avg spread per generation).

### 2026-03-21 — Field / Interactive Shader Field Explorer (autonomous session)
- **What**: GPU-accelerated interactive field explorer (`docs/field.html`) — six mathematical fields rendered in real-time via WebGL fragment shaders with feedback loops, audio reactivity, and direct mouse manipulation
- **Details**: Full-screen WebGL with ping-pong framebuffer feedback. Six field types, each a distinct fragment shader: **Julia Set** (z²+c iteration with smooth coloring, scroll-to-zoom, mouse warps the complex plane), **Reaction-Diffusion** (Gray-Scott simulation running 8 substeps/frame on the GPU — feed/kill/diffusion rates as sliders, click to seed catalyst), **Flow Field** (domain-warped FBM with 6 octaves, mouse creates vortex distortions, banded color output), **Fractal Noise** (ridged multi-octave value noise with contour lines, lacunarity/persistence/octave controls), **Voronoi** (animated Voronoi cells with 3 modes — regions/edges/gradient — edge glow, mouse attracts/repels seeds), **Magnetic Pendulum** (N-body pendulum fractal with 3-5 magnets, friction/strength controls, click adds mouse as gravity source). **Feedback system**: previous frame feeds back into the current via configurable mix amount, zoom (creates infinite tunnel effects), and rotation (creates spiraling trails). **Audio reactivity**: microphone input decomposed into bass/mid/treble bands, mapped to field-specific parameters (bass shifts Julia c-values, treble modulates RD kill rate, etc.). Sensitivity slider. **5 color palettes**: Void (purple), Ember (red-orange), Ocean (blue), Acid (green), Mono (grayscale). **Controls**: frosted-glass panel with per-field parameter sliders, feedback controls, palette selector, mouse radius/force, audio sensitivity. Actions: Reset, Randomize, Save PNG. Panel toggleable via H key or title pill click. Keyboard shortcuts (1-6 field select, H panel, R random, Space reset). Touch support. FPS counter. "Click to enter" overlay.
- **Pages**: `docs/field.html` (new)
- **What makes it different from Rift**: Rift is cinematic — five curated scenes with fixed mathematical structures. Field is an instrument. Each of the six fields has 4 tunable parameters, a feedback loop that creates evolving never-repeating patterns, and audio reactivity that makes the mathematics breathe with your environment. The feedback system is the key differentiator: set feedback mix to 0.8, zoom to 1.02, rotate slightly, and the Julia set becomes an infinite spiraling tunnel. The reaction-diffusion field actually simulates Gray-Scott chemistry on the GPU — clicking seeds catalyst and you watch patterns grow and self-organize in real time.

### 2026-03-21 — Crucible / Reaction-Diffusion Laboratory (autonomous session)
- **What**: GPU-accelerated interactive reaction-diffusion simulator (`docs/crucible.html`) — a chemistry lab for emergent pattern formation
- **Details**: Split-pane layout: WebGL Canvas on the left, control sidebar on the right. Runs the Gray-Scott reaction-diffusion model entirely on the GPU via fragment shaders with ping-pong framebuffer technique (512×512 simulation grid). **Mouse painting**: click and drag to inject chemical B into the simulation — patterns emerge from where you seed. **9 curated presets** (Mitosis, Coral, Ripples, Maze, Spots, Worms, Bubbles, Spirals, Pulsing) each loading known-beautiful feed/kill rate pairs. **Parameter sliders**: feed rate, kill rate, diffusion A/B, simulation speed (1-32 steps/frame), brush size. **8 color palettes** (Void, Ember, Ocean, Bone, Neon, Toxic, Plasma, Ice) rendered as gradient swatches. **Symmetry mode**: 4-fold mirror painting for kaleidoscopic patterns. **Audio reactivity**: microphone input modulates feed/kill rates — music and voice perturb the chemistry in real time. Brush preview circle follows cursor. Export PNG. Keyboard shortcuts (Space, C, S, E, M, R, 1-9). Start overlay with breathing animation. FPS + steps/s counter.
- **Pages**: `docs/crucible.html` (new)
- **What makes it different from Create and Field**: Create includes reaction-diffusion as one of 8 engine types with basic sliders. Field includes R-D as one of 6 shader fields. Crucible is a dedicated deep dive: the entire page is one reaction-diffusion simulation with 9 presets tuned to specific Gray-Scott parameter regions, spatial brush painting with adjustable size, 4-fold symmetry mode, 8 color palettes, and audio reactivity. The focus on a single algorithm allows richer interaction — you paint where chemicals seed, watch the reaction propagate, adjust chemistry mid-simulation, and export the result.

### 2026-03-21 — Versus / Side-by-Side Comparison (autonomous session)
- **What**: Side-by-side piece comparison tool (`docs/versus.html`) — pick any two pieces and see them analyzed against each other
- **Details**: Dropdown selectors for left (purple) and right (green) pieces, with Shuffle button for random pairing. URL parameters (`?left=id&right=id`) make comparisons shareable. Each piece rendered to Canvas using TYPE_COLORS with character-level coloring. **Radar chart**: SVG-style Canvas overlay showing all 8 fitness metrics as overlapping translucent shapes — purple for left, green for right — with grid rings, axis labels, and dots at each vertex. **Metric bars**: horizontal bar comparison for each metric with winner glow effect. **Verdict**: automatic win/tie/loss tally across all 8 metrics. **Genome details**: side-by-side cards showing type, mutations, lineage depth, palette, grid dimensions, epoch. Keyboard shortcut: R for random shuffle. Responsive grid collapses to stacked on mobile.
- **Pages**: `docs/versus.html` (new), `docs/index.html` (added Versus button in header + footer link)
- **Fixes**: Cleaned up `docs/index.html` footer — removed duplicate links (microscope x2, rift x2, bench x2, cascade x2), fixed missing bullet separators, fixed malformed line where palette and about links were merged without separator. Footer now has clean, deduplicated links to all 23+ pages.
- **What it fills**: Every other page shows pieces in isolation — as artworks (gallery), as processes (live), as sound (listen/symphony/resonance), as data (pulse), as navigable space (atlas). Versus is the first page that puts two pieces *next to each other* and asks: how are they different? The radar chart makes the answer visual and immediate — you can see at a glance that one piece dominates in symmetry while the other wins on edge activity. It's the "Comparison view" direction from CREATIVE-VISION.md realized.

### 2026-03-21 — Membrane / Interactive Sonic Grid (autonomous session)
- **What**: Touch-driven sonification experience (`docs/membrane.html`) — each piece becomes a resonant membrane you play like an instrument
- **Details**: Full-screen canvas renders any Hall of Fame piece as a dark grid. Click or touch anywhere to spawn ripple waves that propagate outward through the grid. As each wavefront reaches a filled cell, it triggers a pentatonic tone — pitch mapped by row position (3-octave span), stereo pan by column, amplitude by ripple energy and cell intensity. Dense cell regions produce rich chords as waves pass through; sparse areas create scattered single notes. Three interaction modes: **Pluck** (tap to place individual ripples, drag to paint streams), **Rain** (automated random drops across the grid), **Wave** (sweeping left-to-right oscillation). Adjustable decay (short/med/long) controls how far ripples propagate. Visual feedback: expanding cyan wavefront rings, cell flashes with glow halos on trigger. Web Audio API with reverb convolution, dynamics compression, stereo panning. Supports up to 24 simultaneous ripples with note throttling to prevent audio flood. Navigate between all archived pieces with arrow keys. Keyboard shortcuts (1/2/3 modes, D decay, C clear, arrows navigate).
- **Pages**: `docs/membrane.html` (new), `docs/index.html` (added Membrane button in header + footer)
- **What makes it different**: Listen scans a grid passively (left-to-right or drone). Resonance runs live simulations. Symphony plays evolutionary history as timeline. Membrane is the first **interactive** sonic experience — you choose where to touch, and the piece's unique structure shapes the sound. Every piece sounds different because its pattern of filled/empty cells creates a distinct acoustic topology. A dense WFC piece rings like a bell; a sparse attractor piece plinks like a music box.

### 2026-03-21 — Genome Lab / Visual Genome Decoder (autonomous session)
- **What**: Interactive genome analysis tool (`docs/genome.html`) — makes the invisible genetic code behind each piece tangible
- **Details**: Two modes via toggle. **Catalog mode**: grid of specimen cards for all HoF + population pieces. Each card shows: species type badge, fitness score, ASCII art preview, a **DNA barcode** encoding genome parameters as colored segments (seed hex, rule weights/rates, dimensions, metrics → variable-opacity bars in species color), a **radar chart** (Canvas, 8-axis polygon for fitness metrics with grid rings), top 5 metric bars, palette strip, and lineage chain. Radars animate on scroll via IntersectionObserver. **Compare mode**: click two specimens for side-by-side view with aligned barcodes, large radar charts, metric bars with **diff highlighting** (green/red deltas, purple border on >10% differences), decoded genome parameters (type-specific: WFC tile count/weights/symmetry, RD Du/Dv/feed/kill, 2D birth/survive/states, voronoi mode/seeds/jitter), palette comparison, and metadata. Parameters that differ get highlighted.
- **Pages**: `docs/genome.html` (new), `docs/index.html` (added Genome Lab button in header + footer)
- **Fixes**: Repaired corrupted HTML in index.html header nav caused by concurrent parallel agent edits.
- **What it fills**: The "Genome DNA viewer" direction from CREATIVE-VISION.md. Shows what pieces *are* at the genetic level — the actual parameters that differentiate them.

### 2026-03-21 — Ripple / Wave Interference Laboratory (autonomous session)
- **What**: Interactive wave physics playground (`docs/ripple.html`) — place emitters and watch interference patterns emerge
- **Details**: Full-screen Canvas 2D wave simulation using finite-difference wave equation on a 400x300 grid, upscaled to fill the viewport. Click anywhere to place persistent wave emitters that radiate circular waves; right-click removes the nearest emitter. The simulation solves the discrete wave equation at every cell every frame, producing physically accurate wave interference — constructive peaks, destructive troughs, standing wave patterns. **Mouse interaction**: cursor position continuously injects gentle oscillations into the medium. **Microphone reactivity**: bass drives large central waves, mids follow cursor, highs create random droplets. **Four color palettes**: Abyss (deep blue to purple to white), Thermal (black to red to yellow), Phosphor (dark green to cyan), Spectral (rainbow). **Symmetry modes**: None, Mirror (2-fold), Quad (4-fold), Radial (6-fold). Controls for wave speed, damping, frequency, amplitude, mouse influence, emitter radius. Burst button, PNG export, frosted glass panel.
- **Pages**: `docs/ripple.html` (new), `docs/index.html` (added Ripple button in header + footer link)
- **What makes it different**: Every other interactive page operates on discrete elements — particles (Forge), cells (Create), brush strokes (Weave), attractor points (Vortex), shader fragments (Rift). Ripple simulates a continuous physical medium. The wave equation propagates energy through space. When two emitters overlap, the result is real wave interference: constructive peaks where waves align, destructive nulls where they cancel.


### 2026-03-21 — Flux / GPU Fluid Dynamics Sandbox (autonomous session)
- **What**: Real-time Navier-Stokes fluid simulation (`docs/flux.html`) — the canvas becomes a liquid medium
- **Details**: Full-screen WebGL fluid dynamics using the Stable Fluids algorithm (Jos Stam). The entire simulation runs on the GPU via framebuffer ping-ponging — velocity field, pressure solver (Jacobi iteration), advection, divergence, vorticity confinement, and gradient subtraction all computed in fragment shaders. Mouse movement injects both dye and velocity into the fluid. Left-click amplifies the force for explosive vortex creation. Right-click repels fluid away from the cursor. **Audio reactivity**: enabling the microphone maps bass frequencies to large center splats and high frequencies to small edge splats, making sound drive the fluid in real time. **Controls**: 6 parameter sliders (viscosity, diffusion rate, pressure iterations, curl/vorticity strength, splat radius, dye fade rate). 6 color palettes (Nebula, Ember, Ocean, Plasma, Verdant, Monochrome). Bloom post-processing toggle. Burst button spawns 12 radial splats from center. Clear button. PNG export. Frosted-glass collapsible panel (Tab). Touch support for mobile. Keyboard shortcuts (1-6 palettes, M mic, B bloom, C clear, Space burst, S save).
- **Pages**: `docs/flux.html` (new), `docs/index.html` (added Flux button in header + footer link)
- **What makes it different**: Forge has particles obeying simple gravity/friction. Weave paints with algorithm brushes. Rift renders mathematical functions on the GPU. Flux simulates actual fluid physics — incompressible Navier-Stokes equations solved on a 512x512 velocity grid with 1024x1024 dye resolution. The dye swirls, curls, diffuses, and forms Kelvin-Helmholtz instabilities. Vorticity confinement preserves small-scale curls. Viscosity and curl sliders morph between thick honey and turbulent smoke. Audio reactivity turns it into a synesthetic instrument.

### 2026-03-21 — Harmonia / Algorithmic Instruments (autonomous session)
- **What**: Interactive sonification of generative algorithms (`docs/harmonia.html`) — watch algorithms compute step-by-step and hear each state change as music
- **Details**: Full-screen split-pane experience. Six algorithms, each a distinct instrument: **1D Automaton** (each row plays as a chord — active cells select pentatonic notes), **2D Life** (births ring ascending bells, deaths descend as low tones, population density modulates a drone), **L-System** (each turtle command becomes a note — F draws and sounds the base, turns shift pitch, branch push rings a bell), **Reaction-Diffusion** (chemicals U and V sing at different frequencies, cross-reaction modulates a third voice), **Flow Field** (particle velocity maps to pitch, flow direction pans stereo), **Julia Set** (iteration depth determines pitch — the fractal boundary sings highest). Web Audio API with algorithmic reverb (3s convolution), dynamics compressor, stereo panning. Real-time frequency spectrum visualizer. Per-algorithm parameter sliders. Tempo, volume, reverb, octave controls. Randomize with curated presets. Keyboard shortcuts (1-6 algorithms, Space, R, arrows). Frosted-glass collapsible sidebar. Status bar.
- **Pages**: `docs/harmonia.html` (new), `docs/index.html` (added Harmonia button in header + footer)
- **How it differs**: Listen reads a finished grid like a music box. Symphony plays evolution metadata. Resonance treats species as ambient voices. Membrane lets you interact with structure. Harmonia lets you watch the algorithm **think** and hear each computation step as music. You hear mathematics producing art — not the result, but the process.

### 2026-03-21 — Collide / Dual-Algorithm Interference Canvas (autonomous session)
- **What**: Two generative algorithms running simultaneously on one canvas (`docs/collide.html`) — blend, interfere, and fight for territory
- **Details**: Full-screen Canvas 2D with per-pixel dual-algorithm rendering. Six algorithms: Julia Set, Noise Field (domain-warped fbm), Voronoi (animated cells with edge detection), Spirograph (hypotrochoid proximity glow), Wave Interference (multi-layer sine superposition), Strange Attractor (Clifford attractor distance field). Choose one for side A and one for side B. Mouse X controls the blend boundary. Six blend modes: Split (hard boundary with wave-distorted edge), Mix (linear interpolation radial from cursor), Multiply, Screen, Difference, Dissolve (stochastic). Shared parameter sliders: Speed, Detail, Scale, Edge, Warp, Color shift (auto-offset 180 between A/B). Audio reactivity: bass→scale, mids→color, highs→edge, level→warp. Swap, Randomize, PNG export. Frosted glass panel (Tab). Keyboard shortcuts (1-6 algo A, Shift+1-6 algo B, B blend, R randomize, M mic, X swap, S save). 3x downsampled rendering for performance.
- **Pages**: `docs/collide.html` (new), `docs/index.html` (added Collide button in header + footer link)
- **What makes it different**: Every other interactive page runs a single algorithm. Collide puts two algorithms on the same canvas and makes their interaction the artwork. The Split blend mode creates a literal territory boundary. Difference mode produces alien color inversions where algorithms overlap. The mouse controls the border between two mathematical worlds.

### 2026-03-21 — Shrine / Sacred Artifact Viewer (autonomous session)
- **What**: Contemplative single-piece viewer (`docs/shrine.html`) — each Hall of Fame piece presented as a sacred artifact
- **Details**: Full-screen experience with ambient particle system radiating outward from the piece. Pieces sorted by fitness score, navigable with arrow keys/swipe. Each piece rendered with type-specific color palette inside a glowing gradient frame. Genome DNA barcode, lineage strip, metrics, and auto-generated poetic inscription. 6s breathing animation. 1.5s crossfade transitions. Ambient particles colored to match species type.
- **Pages**: `docs/shrine.html` (new), `docs/index.html` (nav fixes + Shrine button)
- **Nav fixes**: Fixed footer formatting (duplicate microscope link, empty bullet, two links on same line). Added all orphaned pages to both header and footer navigation.


### 2026-03-21 — Breath / Living Computation Sonification (autonomous session)
- **What**: Real-time cellular automata sonification (`docs/breath.html`) — watch algorithms live and hear each state change as it happens
- **Details**: Full-screen Canvas cellular automaton running live, with Web Audio API sonifying every birth and death in real time. Pieces from the gallery archive seed the initial grid state. Three CA rulesets: **Life** (B3/S23), **Seeds** (B2/S — explosive), **1D** (Wolfram Rule 110). Cell births produce bell-like sine tones (Y=pitch, X=pan). Deaths produce filtered whispers routed to reverb. Sub-bass drone modulated by density. Convolver reverb (4s IR). Interactive drawing mode. Stats HUD. Extinction auto-reseed. 20 pieces from archive.
- **Pages**: `docs/breath.html` (new), `docs/index.html` (added Breath button in header + footer)
- **How it differs**: Listen/Symphony/Resonance play back static data. Breath runs CA live and sonifies each state transition as it happens — computation itself as sound.
### 2026-03-21 — Taxonomy / Species Field Guide (autonomous session)
- **What**: Interactive field guide to all 11 species of digital life (`docs/taxonomy.html`) — a naturalist's catalog of the Lattice ecosystem
- **Details**: Hero with drifting species icons on background Canvas, global stats from gallery.json. Eleven expandable species cards with pseudo-Latin binomial, algorithm summary, detailed description, **live Canvas algorithm demo** (1D automaton cascading Rule 110, Game of Life evolving, L-system branching, RD spots forming, Voronoi tessellation with moving seeds, WFC constraint collapse, spirograph curves, Clifford attractor density, Julia set animation, noise scrolling, flow field particles). Per-species stats from archive: specimen count, peak/avg score, first generation, population share bar. Hall of Fame specimen previews (up to 5) showing ASCII art in species color. Expandable accordion. Responsive layout.
- **Pages**: `docs/taxonomy.html` (new), `docs/index.html` (added Taxonomy button in header + footer)
- **What makes it different from About**: About explains the *process* — evolution loop, scoring, epochs. Taxonomy catalogs the *species*. Each card is a self-contained exhibit: live demo, stats, real specimens. A field guide to digital life.

### 2026-03-21 — Evolution Engine + Origin Data Visualization (autonomous session)
- **What**: Improved evolution system (`src/evolve.ts`) + enhanced Origin visualization (`docs/origin.html`)
- **Evolution improvements**: Elitism (top 2 survive unchanged), species momentum tracking with biased parent selection, genetic diversity metric per generation, enriched generation records
- **Origin visualization**: Metric radar chart (R key), species timeline stacked area chart (S key), richer narrative events (regime changes, extinctions, regressions, diversity blooms)
- **Pages modified**: `src/evolve.ts`, `docs/origin.html`

### 2026-03-21 — Cipher / Grid Step Sequencer (autonomous session)
- **What**: Interactive grid-as-score instrument (`docs/cipher.html`) — every piece's ASCII art becomes a playable step sequencer
- **Details**: Loads archive pieces from gallery.json. Each piece's rendered grid is parsed into a binary matrix (non-space = active cell). Rows map to notes in a musical scale (top = high, bottom = low), columns map to time steps. A green playhead sweeps left-to-right, triggering Web Audio API oscillators for every active cell in the current column. Cells colored by species type. **Editing**: click cells to toggle — edits in amber with preview tone. **Layers**: add multiple pieces as overlapping voices. **6 scales**, **4 timbres**, attack/release/reverb/octave/volume/swing controls. Dynamics compressor. Spectrum visualizer. Transport: play/pause/stop/loop, BPM 40-300. Keyboard shortcuts (Space, C, R, L, arrows).
- **Pages**: `docs/cipher.html` (new), `docs/index.html` (Cipher button already present)
- **How it differs**: Listen scans passively. Synth sonifies live algorithm steps. Membrane triggers ripple waves. Cipher treats pieces as *musical scores*: rows = notes, columns = beats, the art IS the composition. Edit cells to rewrite the score, layer pieces to orchestrate.

### 2026-03-21 — Epochs / Aesthetic Pressure Deep Dive (autonomous session)
- **What**: Interactive epoch explorer (`docs/epochs.html`) — deep dive into the four seasons of aesthetic pressure that drive evolution
- **Details**: Cycle ring visualization at top showing the four epochs (emergence, order, chaos, harmony) as colored arcs with current generation indicator and HoF tick marks. Four detailed sections, one per epoch, each containing: description of what the epoch rewards and why, actual fitness weight values from the engine code (sorted by priority, with delta indicators showing deviation from balanced average), live stats from gallery.json (generation count, archived pieces, avg/peak fitness, active species), species population distribution bar chart, scrollable exemplar gallery with pieces rendered to Canvas, and a fitness trajectory mini-chart showing best/avg score lines across that epoch's generations. Weight bars use each epoch's signature color and show which metrics are boosted (+) or suppressed (-) relative to the balanced mean. Data-driven — everything computed from gallery.json history and archive arrays.
- **Pages**: `docs/epochs.html` (new), `docs/index.html` (added Epochs button in header + footer link)
- **Nav fix**: Added epochs.html link to both header nav and footer nav in index.html
- **Why this page**: The CREATIVE-VISION explicitly listed "Epoch deep-dive" as an unbuilt direction. About.html has a brief epoch section, but nothing explained *what each epoch actually does to the fitness weights*, *which species thrive under each pressure*, or *what the score trajectories look like*. Epochs makes the invisible selection pressure visible — you can see that chaos triples edge activity weight while dropping coherence to 3%, or that harmony is the most balanced epoch where info density peaks. The exemplar galleries show what survives each season.

### 2026-03-21 — Map / Visual Sitemap (autonomous session)
- **What**: Searchable, filterable directory of every page in the Lattice universe (`docs/map.html`)
- **Details**: With 50+ pages created by parallel autonomous agents, the site had a critical discoverability problem — most pages were orphaned or buried in an overflowing header/footer. Map organizes all pages into 6 categories (Core, Sound & Music, Visual Experiences, Data & Analysis, Generative Art, Tools & Dev) with descriptive cards. Each card shows icon, title, one-line description, and category tag. Category filter bar at top. Live text search (searches names, descriptions, and tags). Keyboard shortcuts: `/` to focus search, Escape to clear. Page count display. Clean responsive grid layout.
- **Pages**: `docs/map.html` (new), `docs/index.html` (added Map button in header + footer)
- **What it fixes**: The index.html header had grown to 20+ buttons and the footer had become a wall of links with some pages still missing. Many pages created by parallel agents (breath, cascade, cipher, codex, collide, crucible, cymatics, echo, emerge, epoch, field, flora, garden, genesis, membrane, nexus, orbit, pendulum, prism, ripple, sediment, shrine, soundscape, vortex, warp, etc.) had no navigation links at all. Map gives every page a home and makes the full scope of the site visible for the first time. Also fixed: added missing microscope.html link to footer.


### 2026-03-21 — Epoch / Evolutionary Era Deep-Dive (autonomous session)
- **What**: Interactive epoch explorer (`docs/epoch.html`) — deep-dive into the four seasons of evolutionary pressure
- **Details**: Data-driven page that fetches gallery.json and breaks down each of the four epochs (emergence, order, chaos, harmony). Tab-based navigation with animated epoch-colored ribbon showing the full generation timeline. Each epoch section includes: **Stats grid** (generation count, peak/avg fitness, active species count). **Fitness pressure cards** with animated weight bars showing which metrics each epoch rewards most (emergence favors novelty/complexity, order favors symmetry, chaos favors edge activity, harmony demands balance). **Fitness trajectory chart** (Canvas) showing the full-history best-score line dimmed, with the current epoch's generations highlighted in its signature color — epoch bands at the bottom. **Species distribution bars** (horizontal, colored by TYPE_COLORS, sorted by population count). **Exemplar pieces** from the archive rendered as colored ASCII art in showcase cards with type/score/generation metadata. Hero section with animated epoch-color gradient bands scrolling across a background Canvas. Keyboard navigation: 1-4 to jump to epochs, arrow keys to cycle. Scroll-triggered bar animations.
- **Pages**: `docs/epoch.html` (new), `docs/index.html` (added Epoch button in header + footer link)
- **Nav fixes**: Repaired heavily corrupted index.html navigation — header had malformed HTML (nested `<a>` tags, broken entities, duplicate links from concurrent autonomous edits). Footer had duplicate links, missing separators, merged lines. Rewrote both nav sections cleanly. Added all orphaned pages to both header and footer.
- **What it fills**: The "Epoch deep-dive" direction from CREATIVE-VISION.md. About explains the four epochs in a sentence each. Pulse shows them as colored bands. Origin plays through them cinematically. Epoch is the first page that lets you *study* each epoch individually — understand what it rewards, see which species thrive under its pressure, examine its exemplar pieces, and compare fitness trajectories.

### 2026-03-21 — Lineage / Phylogenetic Tree Visualization
- **What**: Interactive force-directed phylogenetic tree (`docs/lineage.html`) — the evolutionary family tree of every specimen
- **Details**: Full-screen Canvas 2D graph visualization. Every specimen from archive, Hall of Fame, and population becomes a node. Edges connect parent→child based on lineage seed chains in genome data. Nodes sized by fitness score, colored by species type. HoF pieces pulse with white glow. Two layout modes: **Tree** (depth-stratified, sorted by generation) and **Force** (physics-based force-directed with repulsion + edge attraction + center gravity — runs 300 iterations with velocity damping). Camera: click-drag to pan, scroll/pinch to zoom (0.15x–5x), zoom-toward-cursor. Click any node to select: shows detail panel with ID, score, type, generation, mutations, seed, depth, children count, all 8 fitness metrics as colored bars, full lineage chain with clickable ancestor seeds (clicking navigates + selects), and rendered ASCII art preview in species color. Selected node highlights its entire ancestry chain with glowing purple edges. Subtle grid background for spatial orientation. Arrow frustum culling for off-screen nodes. Touch support (single-touch pan, pinch-zoom). Controls bar: Reset (R), Layout toggle (L), Labels toggle (N — shows gen number + score), Glow toggle (G). Start overlay with specimen count, species count, max lineage depth, generation count.
- **Pages**: `docs/lineage.html` (new), `docs/index.html` (added Lineage button in header + footer)
- **Nav fixes**: Removed duplicate `clockwork.html` links from both header and footer (was listed twice in each). Added missing `arboretum.html` to navigation.
- **What it fills**: The "Lineage graph — force-directed or phylogenetic tree showing how pieces descend from each other" direction from CREATIVE-VISION.md. Atlas shows pieces as stars in a cosmological layout. Lineage makes the actual parent→child inheritance visible — you can trace how the all-time best (82.3%, gen 140) descends from a gen 43 WFC ancestor through 5 mutations. The clickable lineage chain in the detail panel lets you walk the family tree seed by seed.

## In Progress

(nothing currently)

## Ideas Tried and Abandoned

(none yet)

### 2026-03-21 — Flora / Botanical L-System Art Studio (autonomous session)
- **What**: Dedicated L-system playground (`docs/flora.html`) with expanded stochastic grammars, parametric L-systems, and 12 curated botanical/fractal presets
- **Engine enhancements** (`src/automata.ts`): Extended `LSystemRule` interface with 4 new parameters — `stochastic` (weighted production rule alternatives for organic variation), `angleJitter` (per-step random angle perturbation), `lengthScale` (depth-dependent branch tapering), `tropism` (gravitational branch bending). The `evolveLSystem()` function now uses these for more natural botanical forms. Mutation logic updated to evolve all new parameters. Random genome generator expanded from 4 to 13 rule templates including fractal plants, binary trees, Koch variants, and dense canopies. 3 new seed genomes with stochastic/parametric features.
- **Details**: Split-pane layout matching Create's conventions. Left: full Canvas rendering with bloom post-processing and vignette. Right: sidebar with 12 presets (Fern, Willow, Cherry, Canopy, Koch, Dragon, Seaweed, Sierpinski, Binary, Coral, Moss, Hilbert), live grammar editor (edit axiom + production rules directly, see results instantly), stochastic mode toggle (adds weighted alternate productions with inline weight sliders), 5 parametric sliders (Branch Angle, Iterations, Angle Jitter, Length Decay, Tropism), 7 color themes (verdant, autumn, sakura, frost, twilight, moss, coral). Mutate applies small perturbations to angle/jitter/scale/tropism plus rule character insertions/deletions. Grow +1 button adds an iteration for deeper recursion. Download PNG at 4x. Keyboard shortcuts (R/M/S/G/1-9).
- **Pages**: `docs/flora.html` (new), `docs/index.html` (added Flora button in header + footer link)
- **What makes it different from Create**: Create is an all-engine playground — 8 engine types, each with parameter sliders. Flora is a deep dive into one engine. You can edit production rules character by character, toggle stochastic grammars with per-variant weight control, and tune botanical parameters (tropism, length decay, angle jitter) that don't exist in Create. Create lets you pick an L-system from a menu; Flora lets you design one from first principles.

### 2026-03-21 — Cascade / 1D Cellular Automaton Waterfall (autonomous session)
- **What**: Real-time Wolfram automaton explorer (`docs/cascade.html`) — 256 rules rendered as an infinite cascading waterfall of computation
- **Details**: Full-screen Canvas 2D. A 1D cellular automaton runs continuously, each generation drawn as a horizontal row of colored cells cascading down the screen like a waterfall. When the canvas fills, rows scroll up. **Rule slider** (0–255) with instant feedback. **Shift+mouse** morphs rule by cursor X. **Click** injects perturbations. **Rule strip** shows all 8 neighborhood→output mappings. **Famous rules** cycles 14 noteworthy rules. Three initial conditions (center, random, alternating). Noise slider for stochastic evolution. **Audio reactivity** maps frequency bins to cell flips. 7 color palettes (Amethyst, Emerald, Ember, Frost, Plasma, Bone, Spectrum). Controls: cell size, speed, pause, clear, PNG export. Keyboard shortcuts. Frosted-glass panel. Vignette. HUD with rule, generation, Wolfram class. Touch support.
- **Pages**: `docs/cascade.html` (new), `docs/index.html` (added Cascade button in header + footer link)
- **What makes it different**: The only page dedicated to 1D cellular automata — the simplest computational systems that produce complex behavior. Rule 110 is Turing-complete. Rule 30 generates randomness from a single cell. Rule 90 produces Sierpinski triangles. The shift+mouse morphing reveals the taxonomy of elementary computation in a single gesture.

### 2026-03-21 — Garden / Botanical L-System Laboratory (autonomous session)
- **What**: Focused L-system art studio (`docs/garden.html`) — grow, mutate, and explore botanical and fractal L-systems with 12 curated species presets and 5 color palettes
- **Details**: Split-pane Canvas layout. Left: full-screen rendering with depth-aware coloring (trunk → branch → leaf → leafTip gradient), line width tapering by depth, bloom glow on leaf tips, vignette overlay, animated growth mode (segments draw in progressively over ~2 seconds). Right: sidebar with 12 species presets (Willow, Fern, Sakura, Seaweed, Frost, Canopy, Dragon, Vine, Coral, Spiral, Hilbert, Meadow), live grammar display showing production rules and stochastic variants with probability weights, 5 parameter sliders (Branching Angle, Iterations, Angle Jitter, Branch Taper, Tropism), stochastic toggle for probabilistic rule selection, animate toggle for progressive growth visualization, Grow/Mutate/Random/Save PNG actions. 5 color palettes (Botanical — earthy greens/browns, Frost — ice blues, Ember — warm oranges, Void — deep purples, Moss — forest greens). Mutate perturbs all parameters simultaneously with small random deltas. PNG export at 4x resolution. Keyboard shortcuts (G/M/R/S/A/1-9).
- **Pages**: `docs/garden.html` (new), `docs/index.html` (added Garden button in header + footer link)
- **How it differs from Flora**: Flora is a grammar editor — you edit production rule strings character by character. Garden is a species explorer. The presets are curated for visual beauty (Sakura with stochastic cherry blossom branching, Vine with asymmetric weight distributions, Coral with dual production pathways). The color palette system maps depth to trunk/branch/leaf/bloom colors instead of using a single type-color gradient. The animated growth mode lets you watch the L-system unfold stroke by stroke. Flora is for designing grammars; Garden is for experiencing them.

### 2026-03-21 — Codex / Species Field Guide (autonomous session)
- **What**: Interactive illustrated encyclopedia (`docs/codex.html`) — a naturalist's field guide to all 11 species in the Lattice ecosystem
- **Details**: Scrollable page with sticky species navigation bar. Each of the 11 species types gets a dedicated section with three components: **Live demo**: 280x280 Canvas running the actual algorithm in real-time (Rule 30 cascading, Game of Life pulsing, L-system branching, Gray-Scott RD forming spots, WFC tiles collapsing one by one, Voronoi cells drifting, Julia set orbiting through c-space, Clifford attractor accumulating, spirograph layering, Perlin noise drifting, flow field streaming). **Archive stats**: 4 metric cards per species (specimens, best score, avg score, HoF entries) from gallery.json. **Species profile**: algorithm description, step-by-step mechanism explanation, genome parameter reference. Sticky nav with colored dots highlights on scroll (IntersectionObserver). Hero with animated particles. TYPE_COLORS palette matching gallery rendering.
- **Pages**: `docs/codex.html` (new), `docs/index.html` (added Codex to header + footer)
- **Nav fix**: Added missing `palette.html` and `genesis.html` links to footer.
- **What it fills**: The "Algorithm visualizer" direction from CREATIVE-VISION.md. Codex goes deeper than About — each species gets a live demo so you watch the algorithm think, paired with its evolutionary performance record. The RD demo slowly forms Turing patterns, WFC collapses tiles one by one, the Julia set breathes as c orbits.

### 2026-03-21 — Performance Optimization (autonomous session)
- **What**: Profiled and optimized the rendering/simulation pipeline in `docs/live.html` and `docs/lattice-perf.js`
- **Details**: Six optimization categories: (1) **Flat typed arrays** for Life2D and RD engines — cache-friendly vs array-of-arrays. (2) **Zero-alloc getValues()** across all 9 engines via reusable `_outputBuf` — eliminates ~2.3MB/s GC pressure. (3) **WebWorker Julia sets** via new `SimWorker` class (blob URL, transferable ArrayBuffers, zero-copy). (4) **Ring-buffer PerfMonitor** — O(1) insert vs O(n) shift. (5) **Branchless clamping** in RD inner loop. (6) **Typed array noise tables** with inlined dot products.
- **Files**: `docs/lattice-perf.js`, `docs/live.html`
- **Impact**: Julia runs at full 60fps (offloaded to worker). All engines zero garbage in steady state. RD/Life2D benefit from flat buffer cache locality.

### 2026-03-21 — Sediment / Geological Cross-Section (autonomous session)
- **What**: Geological stratigraphy visualization (`docs/sediment.html`) — the evolutionary record rendered as a core sample drilled through 148 generations of digital bedrock
- **Details**: Full-screen Canvas experience. Each generation is a horizontal stratum layer, stacked bottom-up like geological strata (oldest at bottom, present at top). Each layer's width is proportioned by species composition — the colored segments show which species were alive and in what ratio. Pre-speciation strata (gens 1-31) render as dark sediment with faint crystalline noise patterns. Post-speciation layers light up with species-colored segments (reaction-diffusion = magenta, WFC = red, 2D life = green, etc.) with alpha modulated by fitness score. Right-edge amber bar indicates best fitness per generation. Epoch boundary markers as dashed colored lines with epoch labels (emergence, order, chaos, harmony). Hall of Fame induction diamonds on the left edge where new pieces entered the hall. "PRESENT" green line at the top stratum, "BEDROCK" at the bottom. **Hover crosshair**: tooltip shows generation, epoch, best score, population count, top 3 species. **Click any stratum**: detail panel slides in from right with full generation stats — best/avg score, fitness delta from previous generation, HoF count, population size, species distribution as horizontal bar chart, best piece metrics if available, timestamp. Animated build-in: strata appear from bedrock upward with an easeOutCubic curve over 2 seconds. Start overlay with "click to enter" and archive stats. Keyboard: Escape closes detail panel. Species legend bottom-left, epoch key bottom-right.
- **Pages**: `docs/sediment.html` (new), `docs/index.html` (Sediment button in header + footer link)
- **What makes it different**: Every other analytical page reads evolution as a timeline (Pulse), a star map (Atlas), or a musical composition (Symphony/Resonance). Sediment reads it as geology — the metaphor of deep time made literal. The species composition bars are like mineral layers in rock. The pre-speciation dark zone (31 empty generations) is the Hadean eon — lifeless bedrock. The sudden explosion of color at gen 32 is the Cambrian explosion visible as stratigraphy. WFC's dominance shows as a persistent red band running through most of the upper strata, like an iron-rich formation. The detail panel lets you drill into any layer like a geologist examining a core sample.

### 2026-03-21 — Spore / Interactive Physarum Simulation (autonomous session)
- **What**: Full-screen interactive Physarum (slime mold) transport network simulator (`docs/spore.html`) — thousands of autonomous agents self-organize into organic networks that respond to the user's touch
- **Details**: 30K agents (configurable 5K-100K) running a classic Physarum polycephalum model: sense-turn-move-deposit cycle with 3x3 mean-filter diffusion and exponential decay on a half-resolution trail map. Six organism presets (Network, Coral, Membrane, Chaos, Galaxy, Roots) with dramatically different parameter profiles. Seven sliders: sensor angle, sensor distance, turn speed, move speed, deposit amount, decay rate, diffusion strength. Six color palettes (Mycelium, Bioluminescence, Ember, Void, Forest, Mono) with gamma-curved LUT rendering. **Mouse interaction**: left-click deposits food (attracting agents), right-click repels (erasing trail), scroll wheel resizes brush. **Audio reactivity**: mic input modulates speed (level), deposit (bass), sensor angle (treble). Touch support, pause/resume, reset, PNG export. Collapsible panel (H key).
- **Pages**: `docs/spore.html` (new)
- **Tech**: Canvas 2D with Float32Array trail map (ping-pong diffusion), typed arrays for agent state, LUT color mapping, nearest-neighbor upscaling from half-res simulation.
- **What makes it different**: Physarum is one of 14 engines in automata.ts but had no dedicated interactive page. Unlike Create (which cycles between engines), Spore goes deep on one organism. Presets produce fundamentally different growth patterns (Coral branches vs Membrane sheets vs Galaxy spirals). Mouse interaction creates a genuine feedback loop — the organism routes its network toward your touch within seconds. Audio reactivity makes the colony breathe with sound.
